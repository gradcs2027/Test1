# HANDOFF — تسليم الشغل

> الملف ده بيتحدّث بعد كل خطوة جديدة. اقرأه من فوق لتحت عشان تفهم إحنا فين.
>
> **آخر تحديث:** 2026-08-31

---

## 1. المشروع ايه؟

التعرّف على أفعال الإنسان (Human Action Recognition) من الـ skeleton.

```
فيديو ──> YOLOv8n-pose ──> 17 keypoint لكل frame ──> BiLSTM ──> اسم الحركة
```

- **الـ training data:** NTU-60 (2D skeletons)، الـ split هو Cross-Subject (X-Sub)، حوالي 56,000 عينة
- **الـ model:** Bidirectional LSTM (256 → 128 → 128)
- **الـ inference:** sliding window على الفيديو، كل window 3 ثواني، الـ stride 0.33 ثانية

## 2. الهدف من الشغل الحالي

نثبت إن الـ **LSTM أحسن من الـ baselines الأبسط**، وده بمقارنتها بـ:

| Algorithm | الفكرة |
|---|---|
| **LSTM** | شبكة عصبية اتدرّبت على 56k عينة |
| **DTW** (Dynamic Time Warping) | template matching — بيلوي الزمن عشان يطابق حركتين بأطوال مختلفة |
| **$1 Recognizer** | normalize (resample → rotate → scale → translate) وبعدين مقارنة مباشرة |

المقارنة على **3 فيديوهات**: `vidtest1` (37.5s)، `vidtest2` (28.5s)، `vidtest3` (126.5s).
**الأهم فيهم `vidtest3`** لأنه الأطول والأصعب.

---

## 3. النتايج — الحالة الحقيقية دلوقتي

⚠️ **مهم:** لسه **نتيجة واحدة بس** هي اللي اتقاست فعلاً. الباقي لسه ما اتشغّلش.

| Algorithm | vidtest1 | vidtest2 | vidtest3 |
|---|---|---|---|
| **LSTM** | **71.8%** ✅ مقاسة | ⬜ لسه | ⬜ لسه |
| **DTW** | ⬜ لسه | ⬜ لسه | ⬜ لسه |
| **$1** | ⬜ لسه | ⬜ لسه | ⬜ لسه |

**تفصيل نتيجة LSTM على vidtest1 (الوحيدة المقاسة):**
- Overall accuracy: **71.8%**
- Real recall (الحركات الحقيقية): **72.6%**
- Other recall (السكون / مش حركة): **70.4%**

**الأرقام المتوقعة (تخمين — مش نتايج!):** DTW حوالي 40-50%، $1 حوالي 20-30%.
الأرقام دي مكتوبة كـ placeholders في `FULL_PLAN.md` و `run_all_baselines.py` — **متتقالش للدكتور كأنها نتايج** لحد ما نشغّلها فعلاً.

---

## 4. أهم اكتشاف تقني (ده اللي ضيّع وقت كتير)

لما قلّلنا عدد الكلاسات (من 12 → 4 → 3):
- الـ **accuracy على NTU طلعت** (97.4% → 100%) — منطقي، المسألة بقت أسهل
- بس الأداء على **الفيديو بقى أوحش** ❌

**السبب:** لما الكلاسات تقلّ، الـ softmax بيتشبّع (saturation) — الموديل بيدّي confidence عالي على طول حتى لما يكون غلطان. وده بيكسّر الـ **rejection mechanism** اللي شغّال بـ `CONF_REJECT = 0.60`، فالموديل ما بيبقاش قادر يقول "الحركة دي مش من اللي أنا عارفها".

**الخلاصة:** رجّعنا الـ setup الأصلي بـ 10 كلاسات.

```python
action_names = {
    0: 'drink_water',  7: 'sit_down',    8: 'stand_up',   9: 'clap',
   22: 'wave',        27: 'phone_call', 33: 'rub_hands', 34: 'nod_head',
   39: 'cross_hands', 43: 'touch_head',
}
```

---

## 5. الملفات — ايه بيعمل ايه

### الـ pipeline الأساسي (الـ LSTM)
| الملف | الدور |
|---|---|
| `notebook520b8d324a.ipynb` | النوتبوك الكامل (ده الأصل) |
| `cell2_other_class.py` | تعريف الكلاسات + كلاس `other` |
| `cell3_normalization.py` | تطبيع الـ skeleton (توسيط على الـ mid-hip، قسمة على طول الجذع، resample لـ 30 frame) |
| `cell4_split.py` | تقسيم X-Sub |
| `cell5_model.py` | معمارية الـ BiLSTM |
| `cell6_eval.py` | التقييم على NTU |
| `cell7_video.py` / `cell8_extract.py` | تحميل الفيديو + استخراج الـ keypoints |
| `cell9_smoothing.py` | temporal smoothing + temperature calibration |
| `cell10_timeline.py` | رسم الـ timeline + الـ scoring |

### الـ baselines (الشغل الجديد)
| الملف | الدور |
|---|---|
| `baselines_common.py` | دوال مشتركة: `extract_templates`, `setup_sliding_windows`, `build_windows_for_baseline`, `moving_average_predictions`, `enforce_min_duration`, `score_predictions` |
| `fastdtw_core.py` | **تنفيذ FastDTW** (Salvador & Chan 2007) — coarsening / projection / refinement |
| `notebook_fastdtw.py` | **تشغيل FastDTW على الفيديوهات** ← ده اللي تستخدمه |
| `notebook_dtw.py` | ⚠️ نسخة قديمة، **متعرضش نتيجتها** (شوف تحت) |
| `notebook_dollar1.py` | الـ $1 Recognizer كامل |
| `run_all_baselines.py` | runner بيجمع النتايج ويرسم المقارنة |
| `comparison_results.py` | جدول المقارنة |

### الـ Ground Truth — **استورد من `ground_truth.py` بس**
| الملف | الدور |
|---|---|
| **`ground_truth.py`** | ⭐ **المصدر الواحد.** بيجمّع التلاتة + `check()` للسلامة + `project()` لتضييق المفردات + `shared_labels()` للاختبار عبر-الفيديوهات |
| `vidtest1_ground_truth.py` | 4 حركات — من contact sheets. ⚠️ لسه مش متأكّد من صاحب المشروع |
| `vidtest2_ground_truth.py` | 5 حركات — ✅ متأكّد بالمشاهدة (2026-08-31) |
| `vidtest3_ground_truth_v2.py` | 13 حركة — ✅ متأكّد بالمشاهدة (2026-08-31) |
| ~~`vidtest3_ground_truth.py`~~ | 🚫 **متقاعد — بياناته غلط تماماً.** بيرمي `ImportError` لو حد استورده. شوف قسم 6.8 |

⚠️ **متكتبش جدول ground truth بإيدك في أي ملف تاني.** ده كان متكرر في
9 أماكن وخبّى غلطتين حقيقيتين لشهر. لو عايز مفردات أضيق استخدم `project()`.

### التوثيق
| الملف | الدور |
|---|---|
| `HANDOFF.md` | الملف ده |
| `README.md` | نظرة عامة |
| `BASELINES_README.md` | شرح الـ baselines + خطوات التشغيل |
| `FULL_PLAN.md` | خطة التشغيل على Kaggle |
| `QA.md` | أسئلة الدكتور المتوقعة + إجاباتها |

---

## 6. تفاصيل مهمة للي هيكمّل

**تطبيع الـ skeleton لازم يكون واحد في التدريب والفيديو** — أي اختلاف بيكسّر النتيجة:
1. توسيط على الـ mid-hip
2. قسمة على median طول الجذع
3. resample لـ 30 frame (بـ `linspace` subsampling)

**إعدادات الـ inference:**
- `CONF_REJECT = 0.60` — أقل من كده يترمي كـ `other`
- moving average بـ `k=5`
- `MIN_SEGMENT = 2` — أي segment أقصر من كده يتشال (ما عدا `other`)
- multi-scale windows: 1.5s / 2s / 3s / 4s + mirror TTA
- temperature scaling: grid search من 0.5 لـ 3.0

**فرق مهم:** `vidtest3` بـ **30fps**، بينما `vidtest1` و `vidtest2` بـ **60fps**. خلي بالك في أي حساب بالثواني.

**اصطلاح اللابلز في الـ ground truth:**
- `'other*'` = فترة سكون (واقف/قاعد ثابت) — بتتحسب
- `'?'` = frames مش صالحة (closeup مثلاً) — **مبتتحسبش خالص**

---

## 6.5 مشكلة الـ rejection في الـ baselines ⚠️ (مهمة جداً)

**المشكلة:** مفيش كلاس `other` **متدرّب** في المشروع ده. الرفض في الـ LSTM بيتم
بعتبة ثقة (`CONF_REJECT = 0.60`) — لو الموديل مش واثق، الإجابة تبقى `other`.
(اتجرّب كلاس `other` متدرّب في رن 18/19 وفشل: قال `other` صفر مرة من 77 نافذة.)

**الخطأ اللي كان في `notebook_dtw.py`:** بياخد `min(distances)` على طول، فمستحيل
يقول `other` خالص. والفيديوهات معظمها سكون — `vidtest3` فيه ~120 ثانية من 126.5
هي `other`. يعني الـ DTW كانت هتطلع برقم واطي **مش لأنها ضعيفة، لأننا مانعناها
تجاوب صح**. عرض الرقم ده كمقارنة عادلة = تضليل، والدكتور هيمسكها.

**الحل في `notebook_fastdtw.py`:** عتبة رفض **معايرة من بيانات التدريب**:
1. ناخد 30 عينة من كل كلاس
2. نحسب مسافة FastDTW من كل عينة لـ template بتاع كلاسها
3. العتبة = المئوية 80 من التوزيع ده
4. أي نافذة في الفيديو مسافتها أكبر من العتبة => `other`

كده الرفض مبني على البيانات مش على رقم مختار بالمزاج، وموازي لمنطق الـ LSTM.

---

## 6.6 نتايج FastDTW — القياسات التقنية

الـ implementation **اتتحقق منه محلياً** قبل التشغيل:

| الاختبار | النتيجة |
|---|---|
| نفس السلسلة مع نفسها | مسافة = 0 ✅ |
| إشارة ممطوطة ×2 مقابل الأصلية | مسافة = 0 ✅ (الـ warp شغال) |
| مقابل إشارة عشوائية | مسافة = 65.2 ✅ |
| الفرق عن الـ DTW الكامل عند n=30 | **0.000%** ✅ |

**إثبات الـ O(n) مقابل O(n²) بالأرقام** (34 بعد، radius=1):

| n | خلايا DTW كامل | خلايا FastDTW | النسبة | كسب السرعة |
|---|---|---|---|---|
| 30 | 900 | 391 | 0.43 | 1.41x |
| 60 | 3,600 | 855 | 0.24 | 2.55x |
| 120 | 14,400 | 1,799 | 0.12 | 6.02x |
| 240 | 57,600 | 3,703 | 0.06 | 10.94x |
| 480 | 230,400 | 7,527 | 0.03 | **24.50x** |

**اقرا العمود التاني:** لما `n` تتضاعف، خلايا الـ DTW الكامل بتتربّع (×4)،
بينما خلايا الـ FastDTW بتتضاعف بس (×2). ده الفرق بين O(n²) و O(n) بالأرقام.

**نقطة مهمة للدكتور:** عند n=30 (اللي إحنا شغالين عليه) الـ FastDTW بيدّي
**نفس إجابة الـ DTW الكامل بالظبط** (خطأ 0.000%) وأسرع 1.41x. يعني مفيش أي
تضحية بالدقة مقابل السرعة عند الطول ده.

⬜ **لسه:** الـ accuracy على الفيديوهات — محتاج تشغيل على Kaggle.

---

## 6.7 مسار الـ Few-Shot — FastDTW من غير أي داتاسِت

الدكتور طلب نجرّب FastDTW **من غير داتاسِت**. المسار ده منفصل تماماً عن
NTU: بنقص قصاصات من الفيديوهات نفسها ونستخدمها templates.

### المحاولة الأولى (one-shot) — فشلت، والفشل ده كان مفيد

قصاصة واحدة لكل حركة من vidtest1، مقارنة على المواضع.

| المقياس | النتيجة |
|---|---|
| الدقة الإجمالية | 90.9% |
| خط الأساس ("other" على طول) | **95.7%** |
| استدعاء الحركات الحقيقية | **0.0%** |

يعني الدقة 90.9% كانت **أسوأ من إنك تقول "other" على طول**، والنظام مامسكش
ولا حركة واحدة.

### التشخيص — السبب الجذري

جدول المسافات وضّح إن الـ template الصح كان **الأبعد**:

| نوافذ القعود الحقيقية vs | المسافة |
|---|---|
| rub_hands (وقفة شبه ساكنة) | **62.0** ← الأقرب ❌ |
| stand_up | 73.8 |
| sit_down (الصح) | **81.8** ← الأبعد |

جرّبنا فرضيتين واتأكدنا إنهم **مش** السبب:
- ❌ "التطبيع بيضيّع الإشارة" — لأ، ورك→ركبة المتطبّع بيفصل الوقوف
  (+0.755 / +0.882) عن القعود (+0.398 / +0.563) بوضوح.
- ❌ "المواضع الخام تكفي" — لأ، ارتفاع نص الحوض الخام مشوّش بحركة الشخص
  في الكادر (227.8 / 234.0 / 262.9 / 226.2 / 341.1).

**السبب الحقيقي:** الـ DTW على المواضع بتقيس **الوضعية** مش **الحركة**.
بتجمع بالتساوي على الـ 34 بُعد، وإشارة القعود/الوقوف عايشة في 2-3 أبعاد بس،
فبتغرق في ضوضاء الدراع والراس. النتيجة إن أي template قريب من "الوقفة
المتوسطة" بيبقى **جاذب عام** بيبلع كل النوافذ.

### الإصلاحات الستة (`oneshot_core.py` + `run_oneshot.py`)

| # | المشكلة | الحل | حالة القياس |
|---|---|---|---|
| ١ | مقارنة الوضعية مش الحركة | تمثيل الفروق (velocity) | ✅ اتقاس: stand_up من 0/5 لـ 4/5 |
| ٢ | مثال واحد لكل حركة | بنك few-shot: 3 أمثلة لـ sit_down و 3 لـ stand_up | ⬜ |
| ٣ | نافذة 3s وحركة 1-1.5s | نوافذ متعددة 1.5/2/3s | ⬜ |
| ٤ | مسافة غير مطبّعة بطول المسار | القسمة على طول المسار | ⬜ |
| ٥ | مافيش تمييز ساكن/متحرك | بوابة طاقة الحركة | ⬜ |
| ٦ | عتبة متخمّنة | معايرة leave-one-video-out | ⬜ |

**الأهم هو رقم ٢:** "من غير داتاسِت" **مش معناها مثال واحد**. vidtest1 فيه
قعود مرتين ووقوف مرتين، و vidtest2 فيه مرة كمان — يبقى 3 أمثلة لكل حركة
ببلاش، ولسه من غير أي داتاسِت.

### ⚠️ منع التسريب — لازم الدكتور يشوف ده

كل فيديو بيتقيّم بـ templates من الفيديوهات **التانية** بس، والعتبات
متعايرة على الفيديوهات التانية كمان. **vidtest3 مشافش لا template ولا عتبة
جايين منه.**

### ⚠️ نقطة منهجية: "علّي الـ accuracy" فخ

في vidtest3، 96% من الزمن سكون. فـ "قول other على طول" لوحدها بتدّي 95.7%.
أي رقم دقة إجمالي **لازم** يتقال ومعاه خط الأساس ده، وإلا بيبقى مضلّل.
عشان كده المعايرة بتتم على **F1 على مستوى الحدث** (مسكنا كام حركة من الـ 4
مقابل كام إنذار كاذب) — المقياس ده مايتغشّش.

---

## 6.8 🚨 الـ Ground Truth بتاع vidtest3 كان **غلط** — اقرا ده الأول

`vidtest3_ground_truth.py` (القديم) بيقول إن الفيديو = `sit_down` مرتين +
`stand_up` مرتين + حوالي 120 ثانية سكون.

**ده غلط تماماً.** صاحب المشروع شاف الفيديو وقال المحتوى الحقيقي:
الفيديو فيه **13 حركة مختلفة** ومعظمه حركة مش سكون.

الملف الصح: **`vidtest3_ground_truth_v2.py`** — 13 حركة، 71 ثانية موصوفة،
والفترة 88s→126.5s متعلّمة `'?'` يعني **تتستثنى من الحساب** (مش `other`،
لأن حطّها `other` كان هيبقى اختراع بيانات).

⚠️ **أي رقم اتقاس على vidtest3 قبل 2026-08-31 — LSTM أو DTW أو FastDTW —
لازم يتعاد.** ده يشمل كل أرقام قسم 6.6 و 6.7 الخاصة بـ vidtest3، وكمان
ملاحظة "96% سكون" اللي في قسم 6.5 و "خط الأساس 95.7%".

### التوحيد — `ground_truth.py` بقى المصدر الوحيد

الـ ground truth كان متكرر في **تسع أماكن**، وده السبب الجذري لغلطة
vidtest3 إنها عاشت شهر. اتوحّد في 2026-08-31:

```
ground_truth.py              ← استورد من هنا، مش من الملفات فرادى
  ├── vidtest1_ground_truth.py     (جديد — قبل كده مكانش ليه ملف أصلاً)
  ├── vidtest2_ground_truth.py     (متأكّد من صاحب المشروع)
  └── vidtest3_ground_truth_v2.py  (متأكّد من صاحب المشروع)

vidtest3_ground_truth.py     ← متقاعد، بيرمي ImportError لو حد استورده
```

| الملف | كان فيه | بقى |
|---|---|---|
| `cell10_timeline.py` | نسخة vidtest1 بإيد | `import` |
| `notebook_dtw.py` | نسخة vidtest1 بإيد | `import` |
| `notebook_dollar1.py` | نسخة vidtest1 بإيد | `import` |
| `notebook_fastdtw.py` | نسخة vidtest1 + import اسم **مش موجود** | `import` |
| `notebook_oneshot_fastdtw.py` | import من الملف الغلط | `import` |
| `run_oneshot.py` | نسخة تالتة للتلات فيديوهات | `project()` |
| `run_vidtest2/3.py` | import من ملف واحد | `import` |

`run_oneshot.py` تجربة بكلاسين، فبياخد المفردات المضيّقة بـ
`project('vidtest1', keep=('sit_down','stand_up',...))` — بيسقط الجدول
المرجعي بدل ما يكتب جدول تاني بإيده. **ده اللي بيمنع الغلطة إنها ترجع.**

فيه كمان `check()` بيفحص كل جدول: تداخل فترات، فجوات مش متغطية، بداية
مش من صفر، نهاية مش عند مدة الفيديو. التلات جداول عدّوا نضيف.

### 🐛 غلطة تانية اتكشفت أثناء التوحيد — vidtest2

نسخة `run_oneshot.py` كانت بتقول إن الفترة **2.2–5.9 = `'other*'`**
(سكون متأكّد منه). صاحب المشروع بيقول إن **3–5 = `wave`**.

يعني أي baseline كشف التلويح ده صح كان **بياخد عليه إنذار كاذب**.
وكمان الـ `running` (14–16) مكانتش موجودة خالص، و `phone_call` كانت
18.2–23.6 بدل 18–22.

⚠️ **كل رقم لـ vidtest2 من `run_oneshot.py` لاغي كمان.**

النمط المتكرر: الغلطتين (vidtest2 و vidtest3) اتعملوا بنفس الطريقة —
فترة **مش موصوفة** اتكتبت `'other*'` بدل `'?'`. حطّ فترة مش موصوفة على
إنها "سكون متأكّد منه" = **اختراع بيانات**. الفرق بين الاتنين مش شكلي.

### 🔑 المكسب: الاختبار عبر-الفيديوهات بقى ممكن

`ground_truth.py:shared_labels()` بيطلّع الحركات اللي في أكتر من فيديو:

| الحركة | فين |
|---|---|
| `stand_up` | vidtest1 (2×) · vidtest2 (1×) · vidtest3 (2×) |
| `wave` | vidtest1 (2×) · vidtest2 (1×) · vidtest3 (1×) |
| `sit_down` | vidtest1 (2×) · vidtest2 (1×) |

دي أنضف من أي اختبار عملناه لحد دلوقتي: **template من فيديو، اختبار على
فيديو تاني** — كاميرا تانية، لبس تاني، يوم تاني، مافيش أي تسريب.
مشكلة الـ 0/50 كلها كانت إن الـ template والاختبار من نفس الفيديو.

⚠️ vidtest2 **مافيهوش أي حركة بتتكرر**، يعني مافيش اختبار نظيف جوّاه —
   لازم ييجي من بره.

---

## 6.9 تجربة الـ 13 حركة (`run_vidtest3.py`) — النتيجة الصريحة

المهمة: قصاصة **واحدة** (1–2.5s) من كل حركة كـ template، وتصنيف باقي
الفيديو بأقرب template بمسافة FastDTW. **مافيش أي معامل بيتظبط** — لا عتبة
ولا بوابة. الصدفة = 1/13 = 7.7%.

| النسخة | الدقة | الاختبار النظيف |
|---|---|---|
| مواضع، من غير تصحيح hubness | **27.7%** | 0.0% (0/50) |
| فروق، من غير تصحيح hubness | 22.9% | 0.0% (0/50) |
| مواضع + تصحيح hubness | 23.1% | 0.0% (0/50) |
| فروق + تصحيح hubness | 16.1% | 0.0% (0/50) |
| من غير تنعيم | 14.7% | 0.0% (0/50) |
| من غير تطبيع الشكل | 11.8% | 0.0% (0/50) |

### الرقم اللي بيتقال: **0/50**

الدقة الإجمالية (2–3.6× الصدفة) **مضلّلة**، لأن الـ template والاختبار
جايين من نفس ظهور الحركة في نفس الفيديو. الاختبار الوحيد النظيف ١٠٠٪ هو
`stand_up` و `walking` — الحركتين الوحيدتين اللي بتتكرر — نأخد الـ template
من الظهور الأول ونختبر على الظهور **التاني**.

النتيجة: **صفر من 50** في كل الستة نسخ. مش مسألة معايرة.

### إيه اللي بيتوقّعه بدل الصح

```
stand_up #2 [58-61]  →  turn_on_light / play_pingpong / wave   (حسب النسخة)
walking  #2 [38-40]  →  wear_glasses  / brush_hair             (حسب النسخة)
```

إن كل نسخة تدّي إجابة غلط **مختلفة** = مافيش إشارة أصلاً، مش مجرد انحياز.

### تصحيح الـ hubness — نجح في هدفه وقلّل الدقة

المشكلة: `play_pingpong` كان طالع 100% على نفسه (64/64) وفي نفس الوقت هو
الإجابة الغلط لـ `sitting` **133 مرة**. يعني الـ 100% دي وهمية — هو بس
"جاذب عام" بيبلع كل النوافذ.

الحل المطبّق (`run_vidtest3.py:hubness_correct`): كل template يتقاس بمقياسه
هو — `z = (d - وسيط) / MAD` محسوبين على كل نوافذ الفيديو.
⚠️ ده **مش تسريب**: بيتحسب من المسافات نفسها من غير أي ground truth.

النتيجة: التباس `sitting → play_pingpong` نزل من 133× لـ 37×، و
`play_pingpong` نزل من 100% لـ 42.2%. لكن الدقة الإجمالية نزلت 22.9% → 16.1%.

**التفسير: الدقة اللي ضاعت هي مساهمة الـ artifact نفسه.** الـ 22.9% كانت
متضخّمة بالبج اللي بنصلّحه. الرقم الأقل هو الأصدق.

### ليه بيفشل (تشخيص، مش عذر)

1. **`stand_up` الاتنين مش نفس الحركة فيزيائياً.** الأولى قيام من السرير،
   التانية قيام من على كرسي. نفس اللابل، حركة مختلفة.
2. **مقاسات النوافذ (1.5–3.0s) أطول من الحركات نفسها** (كتير منها 2s)،
   فالنافذة بتشرشح على حركتين. `stand_up #2` [58-61] ملزوقة في `wave`
   [61-63] — وفعلاً `wave` طلعت من أعلى التوقّعات.
3. **مثال واحد لكل كلاس × 13 كلاس** = أصعب إعداد ممكن.
4. **الفروق بقت أوحش من المواضع** (22.9% مقابل 27.7%) — عكس اللي لقيناه
   في مسألة الـ sit/stand بكلاسين. مع 13 حركة مختلفة، الوقفة الساكنة
   نفسها بتحمل معلومة حقيقية.

---

## 6.10 🚨 الـ Ground Truth بتاع vidtest2 اتصحّح كمان — و**النتيجة كويسة**

الـ GT القديم كان: `sit_down` [5.9-6.6] + `stand_up` [7.0-7.6] +
`phone_call` [18.2-23.6] + فترات `other*`.

الصح (من صاحب المشروع، مشاهدة يدوية 2026-08-31):

```
3-5     wave
5-7     sit_down_stand_up   ← نزل وقام، حركة واحدة متصلة
14-16   running             ← حركة جديدة تماماً، ماكانتش في الـ GT القديم
18-22   phone_call
```

الفرق الجوهري: **`running` ماكانتش موجودة خالص**، و `sit_down`/`stand_up`
كانوا متقسّمين لحركتين وهما فعلياً حركة واحدة متصلة، والـ `phone_call`
كانت 18.2-23.6 بدل 18-22.

⚠️ الفترات 0-3s و 7-14s و 16-18s و 22-28.5s متعلّمة `'?'` = تتستثنى من الحساب.

⚠️ `notebook_fastdtw.py:213` و `run_all_baselines.py:33` لسه بيستوردوا الاسم
القديم `ground_truth_segments_vidtest2` اللي **اتشال**. سيبته يقع بصوت عالي
بدل ما يشتغل على داتا غلط. لازم يتحدّثوا للمفردات الجديدة.

### النتيجة — `run_vidtest2.py` (نفس الكود بالظبط بتاع vidtest3)

```
الدقة    83.3%   على 48 عيّنة زمنية
الصدفة   25.0%   (1 من 4)
↑ 3.3× الصدفة

running             100.0%  (9/9)
phone_call          100.0%  (20/20)
wave                 70.0%  (7/10)
sit_down_stand_up    44.4%  (4/9)

التباسات:  sit_down_stand_up ↔ wave  (5× و 3×) — وبس
```

| النسخة | الدقة |
|---|---|
| من غير تطبيع الشكل | **87.5%** |
| كامل (فروق + hubness) | 83.3% |
| مواضع + من غير hubness | 81.2% |
| مواضع بدل الفروق | 79.2% |
| من غير تصحيح hubness | 77.1% |

### ⚠️ التحفّظات — لازم تتقال مع الرقم

1. **48 عيّنة زمنية بس.** 10 ثواني موصوفة من 28.5. الرقم عليه ضجيج كبير —
   غلطة واحدة = 2 نقطة مئوية.
2. **مافيش اختبار نظيف.** مافيش حركة بتتكرر في vidtest2، فالـ template
   والاختبار من **نفس ظهور الحركة**. نفس نقطة ضعف vidtest3.
3. الـ 100% بتاعة `phone_call` طبيعية — 2 ثانية template من حركة 4 ثواني
   ثابتة نسبياً.

### 📌 نتيجتين بيتعاكسوا بين الفيديوهين

| | vidtest2 (4 حركات) | vidtest3 (13 حركة) |
|---|---|---|
| تصحيح hubness | **+6.2** نقطة | **−6.8** نقطة |
| شيل تطبيع الشكل | **+4.2** نقطة | **−11.1** نقطة |
| فروق مقابل مواضع | الفروق أحسن +4.1 | المواضع أحسن +4.8 |

يعني **مافيش إعداد واحد بيكسب في الحالتين**. ده مؤشر إن الطريقة حسّاسة
جداً لعدد الكلاسات وطبيعتها، مش طريقة عامة.

---

## 6.10 ⭐ الاختبار عبر-الفيديوهات (`run_crossvideo.py`) — أنضف رقم عندنا

**template من فيديو، اختبار على فيديو تاني.** كاميرا تانية، لبس تاني،
يوم تاني. مافيش أي تسريب ممكن. اتاح بعد توحيد الـ ground truth.

المفردات = الحركات المشتركة بس: `sit_down` · `stand_up` · `wave`.
التقييم على قصاصات الحركة بس (closed-set) عشان **مانحتاجش عتبة رفض** —
العتبة معامل بيتظبط، وده الفخ اللي وقعنا فيه قبل كده.

### النتيجة الرئيسية

| | الدقة | خط أساس الأغلبية | |
|---|---|---|---|
| **عبر-الفيديوهات** (الافتراضي: فروق) | **41.7%** (5/12) | 41.7% | ❌ صفر تحسّن |
| **جوّه الفيديو الواحد** (الضابط) | 75.0% (6/8) | 50.0% | ✅ |

### 📊 الفرق +33.3 نقطة — ده أهم رقم في القسم ده

نفس الكود بالظبط، الفرق الوحيد **مصدر الـ template**. يعني كل أرقامنا
القديمة (اللي كانت جوّه الفيديو الواحد) كانت متضخّمة بحوالي **33 نقطة**.
الرقم اللي يتقال للدكتور هو 41.7% مش 75%.

⚠️ ولاحظ: 41.7% هي **بالظبط** خط أساس الأغلبية. يعني "قول `stand_up`
على طول" بيدّي نفس النتيجة. **مافيش أي مهارة متعلّمة.**

### الدلالة الإحصائية — القيد الحقيقي

| النسخة | قصاصة | p مقابل الأغلبية | نافذة |
|---|---|---|---|
| فروق + تطبيع شكل ⭐ (المختار مسبقاً) | 41.7% | 0.61 | 22.9% |
| **مواضع بدل الفروق** | **58.3%** | 0.19 | **64.2%** |
| من غير تطبيع الشكل | 33.3% | 0.81 | 51.4% |
| مواضع من غير تطبيع | 58.3% | 0.19 | 66.1% |

**حتى أحسن نسخة (58.3%) مش دالة إحصائياً** — احتمال 0.19 إنها تحصل
بالصدفة. بـ 12 عيّنة مينفعش نقول إن الطريقة "نجحت".

🔬 **السقف:** عندنا **12 ظهور حقيقي للحركة** في التلات فيديوهات كلهم.
ده سقف الإحصاء ومفيش بروتوكول بيزوّده. بروتوكول النوافذ بيدّي 109
عيّنة بس **دي مترابطة ومتقصوصة من نفس الـ 12** — ماتتحسبش 109 قياس
مستقل في أي اختبار دلالة. مكتوبة صريح في `window_protocol.__doc__`.

### 📌 المواضع أحسن من الفروق — التكرار التالت

| التجربة | مواضع | فروق |
|---|---|---|
| vidtest3، 13 حركة | 27.7% | 22.9% |
| عبر-الفيديوهات، قصاصة | 58.3% | 41.7% |
| عبر-الفيديوهات، نافذة | 64.2% | 22.9% |

ده **مش اختيار بعد رؤية النتيجة** — المواضع كانت أحسن في تجربة vidtest3
المستقلة قبل التجربة دي. لكن الإعداد الافتراضي المسجّل مسبقاً هو الفروق،
فالـ 41.7% هي الرقم النظيف والـ 58.3% لازم تتقال إنها اختيار لاحق.

⚠️ ده **يخالف** تشخيص قسم 6.7 (الفروق أحسن من المواضع). التشخيص ده كان
صح لمسألة sit/stand بكلاسين، وغلط لما المفردات تتوسّع. السبب المعقول:
مع حركات متنوعة، الوضعية الساكنة نفسها بتحمل معلومة تمييزية حقيقية.

### `sit_down` بيفشل تماماً — 0% في كل نسخة

0/3 قصاصات، 0/23 نافذة. بيتلبّس بـ `stand_up` أساساً. وده متوقّع بعد
تشخيص قسم 6.7: القعود والوقوف نفس مسار الجسم بالظبط، الفرق **الاتجاه**
بس. الـ DTW بتلوي الزمن — وليّ الزمن **بيقدر يقلب الاتجاه**، فهي
معماريّاً مش قادرة تفرّق بينهم بسهولة.

### 📣 اللي ده بيعنيه للمقارنة مع الـ LSTM

ده **بيدعم فرضية المشروع**. الـ FastDTW من غير داتاسِت، لما تتقاس
بأمانة عبر الفيديوهات، بتقعد **عند خط أساس الأغلبية** ومش قابلة
للتمييز عنه إحصائياً. الـ LSTM المتدرّبة على 56k عينة بتاخد 71.8%.
المقارنة دي عادلة ونظيفة ومش قابلة للطعن.

---

## 6.11 ✅ مراجعة الـ ground truth بالفيديو (`render_gt.py`)

الـ ground truth بتاع vidtest1 كان مكتوب من contact sheets مش من مشاهدة
(`verified_by_owner: False`)، وهو **مصدر معظم الـ templates** في اختبار
قسم 6.10. فأي غلطة فيه بتنتقل لكل النتايج. اتعمل `render_gt.py` اللي
بيرسم على كل فيديو: الهيكل العظمي + اسم الحركة من الجدول + شريط زمني
تحت بكل الفترات ومؤشر متحرك. المخرجات `results/gt_vidtest{1,2,3}.mp4`.

### النتيجة: الجدول **صح**

اتراجعت الأربع انتقالات بـ contact sheets كل 0.4s من الفيديو الأصلي:

| الحركة | الجدول | الحقيقي من الصور | الحكم |
|---|---|---|---|
| sit_down #1 | 12.2-14.3 | 12.2 → ~14.0 | ✅ |
| stand_up #1 | 19.5-20.4 | 19.4 → ~20.1 | ✅ (النهاية متأخرة ~0.3s) |
| sit_down #2 | 28.4-29.7 | 28.4 → ~29.5 | ✅ |
| stand_up #2 | 32.4-33.4 | 32.4 → ~33.5 | ✅ |

كلهم في حدود ±0.3s. **الـ 41.7% مش سببها annotation غلط.**

### ⚠️ تصحيح فرضية كانت غلط

من `results/seg.txt` لوحظ إن الـ LSTM بيولّع `stand_up` عند 18.7 و 31.3
بينما الجدول 19.5 و 32.4 — انحياز ~1s في نفس الاتجاه المرتين، والفرضية
كانت إن **بدايات الجدول متأخرة**. الصور أثبتت العكس: عند 19.2 لسه قاعد
تماماً، والحركة فعلاً بتبدأ 19.4-19.5. يعني **الـ LSTM هو اللي بيولّع
بدري** (تأخير نوافذه)، والجدول صح. الفرضية اتشالت.

### 🔍 اكتشاف جديد — دليل بصري على ليه sit/stand بيتلبسوا

في `stand_up` عند 32.4 و 32.8 الشخص **مايل لقدام وإيديه على فخاده**.
وفي `sit_down` عند 12.8 و 13.2 **نفس الوضع بالظبط**. الحركتين بتعدّوا من
نفس أشكال الجسم، والفرق الوحيد **ترتيب الزمن**. ده الدليل المصوّر على
تشخيص قسم 6.10: الـ DTW بتقارن تسلسل أشكال ووليّ الزمن بيقدر يعيد
الترتيب — فهي معماريّاً مش شايفة فرق. مش مسألة تظبيط معاملات.

### 🔍 كمان: نفس اللابل بسرعات مختلفة

sit_down #1 حوالي 1.8s (بطيء وانحناء عميق) بينما #2 حوالي 1.1s.
stand_up #1 حوالي 0.6s بينما #2 حوالي 1.1s. يعني نفس الاسم بضعف السرعة.
ده مصدر صعوبة حقيقي تاني ومستقل عن الأول.

### 🐛 حجم الملفات — اتحل

**المشكلة:** أول تشغيلة طلّعت 175 + 131 + 219 = **525 MB**. السبب إن
الـ OpenCV المتجمّع مع pip مافيهوش H.264 شغّال — `cv2.VideoWriter` بـ
`avc1` بيفشل بـ `Could not open codec libopenh264 (-1313558101)` وبيقع
صامت على `mp4v` اللي ضغطه ضعيف جداً. وده كان بيتخطّى حد الـ 100 MB
للملف الواحد على GitHub.

**الحل:** `pip install imageio-ffmpeg` (31 MB، فيه ffmpeg متجمّع)،
و`render_gt.py` بقى بيبعت الفريمات لـ ffmpeg على الـ stdin مباشرة
(`libx264`, `crf=23`, `yuv420p`, `+faststart`) بدل `cv2.VideoWriter`.
مافيش ملف وسيط ضخم. لو ffmpeg مش موجود بيرجع لـ mp4v ويطبع تحذير.

⚠️ `libx264` بـ `yuv420p` محتاج **أبعاد زوجية** — فيه سطر بيقصّ
`w -= w % 2` قبل الكتابة، من غيره بيفشل على أي فيديو ببُعد فردي.

| الملف | قبل | بعد |
|---|---|---|
| `gt_vidtest1.mp4` | 175 MB | **6.2 MB** |
| `gt_vidtest2.mp4` | 131 MB | **4.5 MB** |
| `gt_vidtest3.mp4` | 219 MB | **18.5 MB** |
| `annotated_output.mp4` (الـ LSTM، قديم) | 64 MB | **10.4 MB** |
| **إجمالي `results/`** | 589 MB | **39 MB** |

الجودة مافيهاش فرق ملحوظ. عدد الفريمات اتأكّد إنه متطابق قبل الاستبدال.

### التخطيط — الشرايط فوق وتحت مش فوق الصورة

أول نسخة كانت بترسم شريط اللابل **فوق الفيديو** فبيغطي أول 96 بكسل منه.
اتغيّر: الـ canvas بقى `HEAD_H + ارتفاع_الفيديو + BAR_H`، والفيديو
بيتحط في النص من غير ما يتغطى بأي حاجة غير الهيكل.

⚠️ **الفخ هنا:** الـ keypoints بإحداثيات الفيديو الأصلي، والفيديو بقى
منزّل `HEAD_H` بكسل لتحت. `draw_skeleton()` بتاخد `y_off` وبتزحزح كل
النقط — من غيرها الهيكل بيتزنق في الشريط العلوي وميبقاش على الجسم.

المقاسات النهائية: `576×1204` للطوليين، `852×660` لـ vidtest3.

---

## 7. الخطوات الجاية

```
☐ 1. شغّل FastDTW على vidtest1  ← الخطوة الجاية دلوقتي
☐ 2. شغّل FastDTW على vidtest2 و vidtest3
☐ 3. شغّل LSTM على vidtest2 و vidtest3
☐ 4. شغّل $1 على الـ 3 فيديوهات
☐ 5. حط النتايج الحقيقية في run_all_baselines.py وشيل الـ placeholders
☐ 6. اعمل الجدول النهائي للدكتور
```

**تشغيل الـ FastDTW على Kaggle:**
```python
# بعد ما تشغّل خلايا الـ notebook الأصلي (عشان X_train_sk و all_keypoints يبقوا متحملين)
exec(open('notebook_fastdtw.py').read())
```

عشان تغيّر الفيديو، عدّل `VIDEO_NAME` في أول `notebook_fastdtw.py`
(`'vidtest1'` / `'vidtest2'` / `'vidtest3'`). الـ fps بيتظبط لوحده.

---

## 8. لوج الشغل (بيتزوّد مع كل خطوة)

### 2026-08-30
- ✅ رجّعنا الـ setup الأصلي بـ 10 كلاسات بعد ما اكتشفنا مشكلة الـ softmax saturation
- ✅ اتكتبت الـ baselines: `baselines_common.py`, `notebook_dtw.py`, `notebook_dollar1.py`, `run_all_baselines.py`
- ✅ اتصحّح `vidtest2_ground_truth.py` (الـ `phone_call` كانت مقسومة غلط)
- ✅ اتعمل `vidtest3_ground_truth.py` — اتستخرج يدوي من contact sheets (overview كل 3 ثواني + zoom كل 0.5 ثانية)
- ✅ اتعمل push للنوتبوك على Kaggle (version 28)
- ✅ اتعمل push للفولدر كله على GitHub → `gradcs2027/Test1`
- ✅ اتعمل `HANDOFF.md` (الملف ده)
- ✅ **اتكتب الـ FastDTW** — `fastdtw_core.py` + `notebook_fastdtw.py`
  - اتتحقق من صحته محلياً: خطأ 0.000% مقابل الـ DTW الكامل عند n=30
  - اتقاس الـ speedup: من 1.41x عند n=30 لـ 24.5x عند n=480
  - اتضافت عتبة رفض معايرة من البيانات (شوف قسم 6.5)
- 🐛 اتصلّح بج في `notebook_dtw.py`: الـ progress print كان بره اللوب فبيطبع رقم غلط
- ⚠️ اتحطّ تحذير في `notebook_dtw.py` إن نتيجته مضللة (مفيش rejection)
- ✅ اتعمل `pose_extract.py` واستخرجنا الـ keypoints محلياً للـ 3 فيديوهات
  - vidtest1: 1125 فريم | vidtest2: 854 فريم (اكتشاف 96.6%) | **vidtest3: 1896 فريم (اكتشاف 100%)**
  - يعني بقى عندنا مسار شغّال محلياً من غير Kaggle ومن غير NTU خالص
- ✅ اتعمل `skeleton_norm.py` — نسخة مستقلة من دوال التطبيع
  (السبب: `cell3_normalization.py` فيه كود بيشتغل على مستوى الموديول فمينفعش يتعمله import)
- ❌ **المحاولة الأولى للـ one-shot فشلت** — دقة 90.9% مقابل خط أساس 95.7%،
  واستدعاء الحركات **0.0%**. التشخيص الكامل في قسم 6.7
- ✅ اتعمل `oneshot_core.py` + `run_oneshot.py` — الستة إصلاحات (قسم 6.7)
  - مافيش تسريب: كل فيديو بـ templates وعتبات من الفيديوهات التانية بس
  - المعايرة على F1 مستوى الحدث مش الدقة الإجمالية (الدقة قابلة للغش هنا)
- 🐛 **اتصلّح بج مهم في `baselines_common.py:moving_average_predictions`**
  - كانت بتاخد متوسط حسابي لأرقام الفئات — بتمسح الكشف الحقيقي وتخترع لابل مكانه
  - مثال: `['other','other','stand_up','other','other']` كانت بتطلع لابل ماحدش توقّعه
  - اتغيّرت لتصويت أغلبية
  - ✅ **الـ LSTM مش متأثرة** — `cell9_smoothing.py:smooth_probs` بينعّم
    الاحتمالات لكل كلاس وبعدين argmax، وده الصح. البج كان في الـ baselines بس،
    يعني كان بيخلّيها تبان أوحش من الحقيقة (محاباة للـ LSTM في المقارنة)

### 2026-08-31
- 🚨 **اتكتشف إن `vidtest3_ground_truth.py` غلط تماماً** — صاحب المشروع شاف
  الفيديو. مش sit/stand + سكون، ده **13 حركة مختلفة**. التفاصيل في قسم 6.8
  - ✅ اتعمل `vidtest3_ground_truth_v2.py` بالتوقيتات الحقيقية
  - ⚠️ **كل أرقام vidtest3 السابقة لازم تتعاد** (LSTM و DTW و FastDTW)
- ✅ اتعمل `run_vidtest3.py` — تجربة few-shot بـ 13 كلاس، قصاصة واحدة لكل حركة
  - الدقة 27.7% مقابل صدفة 7.7% (3.6×) — **بس الرقم ده مضلّل**
  - **الاختبار النظيف ١٠٠٪ = 0/50 في كل الستة نسخ.** ده الرقم اللي يتقال
- ✅ اتعمل تصحيح الـ hubness (`hubness_correct`) — شال artifact الـ
  `play_pingpong` الجاذب العام (التباس 133× → 37×)، والدقة نزلت 22.9% → 16.1%
  لأن الدقة اللي ضاعت كانت مساهمة الـ artifact نفسه
- 📌 اكتشاف يخالف اللي فات: **المواضع بقت أحسن من الفروق** مع 13 حركة
  (27.7% مقابل 22.9%) — عكس مسألة الـ sit/stand بكلاسين
- ✅ **اتوحّد الـ ground truth في مصدر واحد** — `ground_truth.py`
  - كان متكرر في **9 أماكن**، منها 5 نسخ متطابقة لـ vidtest1
  - اتعمل `vidtest1_ground_truth.py` (مكانش ليه ملف أصلاً)
  - `vidtest3_ground_truth.py` (الغلط) بقى بيرمي `ImportError` بدل ما
    يسرّب بيانات غلط بالسكوت
  - 🐛 `notebook_fastdtw.py:213` كان بيستورد اسم **مش موجود**
    (`ground_truth_segments_vidtest2`) — الاستيراد ده كان بيقع
  - 🐛 `vidtest3_ground_truth.py` كان فيه `print` على مستوى الموديول
    بيرمي `UnicodeEncodeError` بمجرد الـ import على ويندوز
  - اتضاف `check()` بيفحص التداخل والفجوات والتغطية — التلاتة عدّوا
  - ✅ اتأكدنا إن التوحيد ماغيّرش أي نتيجة: `run_vidtest3.py` طلّع نفس
    الأرقام بالظبط (16.1% / 0-50)
- 🐛 **اتكشفت غلطة تانية في GT بتاع vidtest2** أثناء التوحيد
  - الفترة 2.2–5.9 كانت `'other*'` (سكون) وهي فيها الـ `wave` (3–5)
  - يعني الكشف الصح للتلويح كان بيتحسب **إنذار كاذب**
  - الـ `running` مكانش موجود، و `phone_call` كانت 18.2–23.6 بدل 18–22
  - ⚠️ كل رقم لـ vidtest2 من `run_oneshot.py` لاغي
- ✅ **صاحب المشروع أكّد تقسيم 5–6 قعد / 6–7 قام** في vidtest2
- 🔑 التوحيد فتح **الاختبار عبر-الفيديوهات**: `stand_up` و `wave` في
  التلات فيديوهات، و `sit_down` في اتنين. template من فيديو واختبار على
  التاني = مافيش تسريب، وده أنضف من أي اختبار عملناه لحد دلوقتي
- ✅ **اتعمل واتشغّل `run_crossvideo.py`** — أنضف اختبار في المشروع (قسم 6.10)
  - **عبر-الفيديوهات = 41.7%، وخط أساس الأغلبية = 41.7% بالظبط.** صفر مهارة
  - **جوّه الفيديو الواحد = 75.0%** → الفرق **+33.3 نقطة**، وده بيقيس
    كمّياً قد إيه كل أرقامنا القديمة كانت متضخّمة
  - حتى أحسن نسخة (مواضع، 58.3%) **مش دالة إحصائياً** — p=0.19 على n=12
  - 🔬 السقف: **12 ظهور حقيقي للحركة** في التلات فيديوهات كلهم
  - 🐛 اتصلّح بج في بروتوكول النوافذ: مقاس النافذة كان = طول الحركة،
    فالمركز مكانش له مساحة يتحرّك فيها → طلّع 12 عيّنة زي بروتوكول
    القصاصة بالظبط بدل ما ينزلق. اتحل بمقاسات نسبية (0.6/0.8/1.0)
  - 📌 **المواضع أحسن من الفروق — التكرار التالت** (58.3% مقابل 41.7%).
    ده يخالف تشخيص قسم 6.7 اللي كان صح لكلاسين وغلط للمفردات الأوسع
  - ❌ `sit_down` = **0% في كل نسخة** (0/3 قصاصات، 0/23 نافذة). السبب
    معماري: القعود والوقوف نفس المسار والفرق الاتجاه، وليّ الزمن في
    الـ DTW بيقدر يقلب الاتجاه
  - 📣 النتيجة دي **بتدعم فرضية المشروع**: الـ FastDTW من غير داتاسِت
    بتقعد عند خط أساس الأغلبية، والـ LSTM بتاخد 71.8%
- 🚨 **`vidtest2_ground_truth.py` اتصحّح كمان** — `running` [14-16] ماكانتش
  موجودة خالص، و sit/stand كانوا متقسّمين وهما حركة واحدة. التفاصيل في 6.10
  - ⚠️ اتشال الاسم القديم `ground_truth_segments_vidtest2` —
    `notebook_fastdtw.py` و `run_all_baselines.py` هيقعوا لحد ما يتحدّثوا
- ✅ اتعمل `run_vidtest2.py` — **83.3% مقابل صدفة 25% (3.3×)** على 4 حركات
  - أحسن نتيجة few-shot لحد دلوقتي، بس على 48 عيّنة بس ومن غير اختبار نظيف
- 📌 **مافيش إعداد واحد بيكسب في الفيديوهين** — تصحيح الـ hubness بيساعد
  في vidtest2 (+6.2) ويضر في vidtest3 (−6.8). جدول المقارنة في 6.10
- ✅ اتعمل `render_gt.py` — بيرسم الـ ground truth على الفيديوهات
  (هيكل + اسم الحركة + شريط زمني) لمراجعتها بالعين. التفاصيل في 6.11
  - المخرجات: `results/gt_vidtest{1,2,3}.mp4`
  - ✅ **جدول vidtest1 اتراجع وطلع صح** — الأربع انتقالات كلها في حدود
    ±0.3s من الحقيقة. يعني الـ 41.7% في 6.10 **مش سببها annotation غلط**
  - ⚠️ **فرضية اتشالت**: كان متفكّر إن بدايات `stand_up` في الجدول
    متأخرة ~1s (من انحياز الـ LSTM في `seg.txt`). الصور أثبتت العكس —
    الجدول صح والـ LSTM هو اللي بيولّع بدري
  - 🔍 **دليل مصوّر على التباس sit/stand**: في `stand_up` عند 32.4-32.8
    الشخص مايل لقدام وإيديه على فخاده، ونفس الوضع بالظبط في `sit_down`
    عند 12.8-13.2. الحركتين بيعدّوا من نفس الأشكال والفرق ترتيب الزمن بس
  - 🔍 نفس اللابل بسرعات مختلفة: sit_down ‏1.8s مقابل 1.1s،
    stand_up ‏0.6s مقابل 1.1s — مصدر صعوبة تاني مستقل
  - 🐛 **اتصلّح**: أول تشغيلة طلّعت 525 MB لأن الـ OpenCV مافيهوش H.264
    شغّال (`libopenh264` بيفشل) فوقع صامت على `mp4v`. اتحل بـ
    `pip install imageio-ffmpeg` و`render_gt.py` بقى بيبعت الفريمات
    لـ ffmpeg على الـ stdin (`libx264`, crf=23). **525 MB → 25.8 MB**
  - ✅ `annotated_output.mp4` القديم (الـ LSTM) اتضغط كمان: 64 → 10.4 MB.
    اتأكّد إن عدد الفريمات متطابق (2249) قبل الاستبدال
  - 📦 `results/` كله بقى **35 MB** بدل 589 MB — بقى ينفع يترفع على GitHub
