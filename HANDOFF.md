# HANDOFF — الحالة دلوقتي

> الملف ده بيوصف **الوضع الحالي**: ايه الشغّال، ايه الأرقام المتقاسة فعلاً،
> وايه اللي لسه ناقص. مش تاريخ ولا لوج.
>
> للشرح التفصيلي وأسئلة المناقشة: `README.md`
> **آخر تحديث:** 2026-09-01

## الفولدرات — كل طريقة لوحدها

```
kaggle_nb_520b8d324a/
├── shared/     الكود المشترك — مسارات · ground truth · تطبيع · قياس
├── lstm/       ⭐ الطريقة الأساسية: BiLSTM على NTU-60
├── fastdtw/    خط الأساس اللي الدكتور طلبه (من غير داتاسِت)
└── dollar1/    خط الأساس التالت — ⬜ لسه ماتشغّلش
```

**كل فولدر فيه `HANDOFF.md` بتاعه وفيه `results/` بتاعه.** الملف ده
بيدّي الصورة الكاملة؛ لو شغّال على طريقة واحدة اقرا الـ HANDOFF بتاعها:

| | |
|---|---|
| [`shared/HANDOFF.md`](shared/HANDOFF.md) | المسارات · الـ ground truth · القواعد المشتركة |
| [`lstm/HANDOFF.md`](lstm/HANDOFF.md) | الـ BiLSTM · الخلايا · إعدادات الـ inference |
| [`fastdtw/HANDOFF.md`](fastdtw/HANDOFF.md) | الـ few-shot · الأرقام النظيفة · بق الكاش |
| [`dollar1/HANDOFF.md`](dollar1/HANDOFF.md) | إيه اللي مانعه يشتغل |

⚠️ **مافيش `results/` واحد.** كل طريقة بتكتب في `results/` بتاعها عشان
محدش يبص على فولدر ويلاقي مخرجات تلات طرق مخلوطة ومايعرفش مين طلّع إيه.

---

## 1. المشروع في سطرين

التعرّف على حركة الإنسان من الهيكل العظمي:

```
فيديو ──► YOLOv8n-pose ──► 17 نقطة لكل فريم ──► BiLSTM ──► اسم الحركة
```

**الهدف من الشغل الحالي:** نثبت إن الـ LSTM المتدرّبة أحسن من الطرق الأبسط
(DTW / FastDTW / $1 Recognizer) على نفس الفيديوهات.

الفيديوهات: `vidtest1` (37.5s) · `vidtest2` (28.5s) · `vidtest3` (126.5s).

---

## 2. الأرقام

⬜ = لسه ماتشغّلش. **مافيش رقم مخمّن أو متوقّع في الملف ده** — الموجود متقاس بس.

### أ) الـ LSTM — متدرّبة على NTU-60 (56 ألف عينة)

| | vidtest1 | vidtest2 | vidtest3 |
|---|---|---|---|
| الدقة | **71.8%** | ⬜ | ⬜ |
| خط الأساس (`other` للكل) | 35.4% | | |
| real recall | 72.6% | | |
| other recall | 70.4% | | |

7 حركات من 8 اتعرفت بالكلاس الصح. الغلط الوحيد: `rub_hands` اتقالت `clap`.

### ب) الـ FastDTW من غير أي داتاسِت — **أنضف رقم عندنا**

الفكرة: بدل ما نتدرّب على NTU، بناخد قصاصة من حركة في فيديو ونستخدمها
`template`، وبنقيس المسافة بينها وبين باقي الفيديوهات.

`run_crossvideo.py` — **template من فيديو، اختبار على فيديو تاني**
(كاميرا تانية، لبس تاني، يوم تاني = مافيش أي تسريب):

| | الدقة | خط أساس الأغلبية | |
|---|---|---|---|
| **عبر-الفيديوهات** | **41.7%** (5 من 12) | 41.7% | ❌ صفر تحسّن |
| جوّه الفيديو الواحد (للمقارنة) | 75.0% (6 من 8) | 50.0% | ✅ |

**الرقم اللي يتقال للدكتور هو 41.7%.**

- 41.7% دي **بالظبط** خط أساس الأغلبية — يعني "قول `stand_up` على طول"
  بيدّي نفس النتيجة. مافيش مهارة متعلّمة.
- الفرق **+33.3 نقطة** بين "جوّه الفيديو" و"عبر الفيديوهات" هو نفس الكود
  بالظبط والفرق الوحيد مصدر الـ template. يعني أي رقم قديم اتقاس جوّه
  الفيديو الواحد كان متضخّم بحوالي 33 نقطة.

### ج) vidtest4 — تنبؤ أعمى (`run_vidtest4.py`)

فيديو رابع، 75.2s، **1232x576 عرضي** (أول فيديو عرضي — التلاتة اللي فاتوا
طوليين). الـ templates كلها من vidtest1+2+3، و vidtest4 **مشافش ولا
template ولا معامل واحد منه**.

⚠️ **مافيش رقم دقة** — الفيديو لسه من غير ground truth. اللي بيطلع خط
زمني متوقَّع بس، عشان يتشاف بالعين.

اللي **ينفع** يتقاس من غير ground truth هو **ثبات التنبؤ**:

| النسخة | عدد الفترات في 75s | وسيط مدة الفترة |
|---|---|---|
| كامل (فروق + hubness) | 64 | 0.85s |
| من غير تصحيح hubness | 51 | 1.19s |
| مواضع بدل الفروق | 47 | 1.19s |

**64 فترة في 75 ثانية = حركة جديدة كل 1.2 ثانية.** مفيش فيديو حقيقي
لواحد بيتحرك كده — ده **ترجرج** مش كشف. عيّنة من الخرج:

```
39.0 - 40.7s   play_pingpong
41.9 - 42.4s   sit_down
42.4 - 42.9s   wave
42.9 - 44.5s   stand_up
44.5 - 46.5s   sit_down
```

خمس حركات في 7 ثواني. النتيجة دي **متسقة** مع الـ 41.7% بتاعة
`run_crossvideo.py` — الاتنين بيقولوا نفس الحاجة: **الطريقة مابتعمّمش على
فيديو جديد**، والأرقام العالية (75%، 83.3%) كانت كلها من templates جوّه
نفس الفيديو.

⚠️ تحفّظ لازم يتقال: المفردات محدودة بـ **18 حركة** من الفيديوهات التلاتة.
أي حركة في vidtest4 مش منهم **مستحيل** تتظبط. لو طلع إن محتوى vidtest4
مختلف، جزء من الترجرج ده متوقّع ومش عيب في الخوارزمية.

📌 اكتشاف جانبي: **اكتشاف الهيكل 93.9%** — أقل واحد في المشروع (vidtest1
و 3 = 100%، vidtest2 = 96.6%). أغلب الظن بسبب الاتجاه العرضي.
- أحسن نسخة (مواضع بدل فروق) بتدّي 58.3%، **بس مش دالة إحصائياً**
  (p = 0.19 على 12 عيّنة) — ومختارة **بعد** ما شفنا النتيجة، فمينفعش
  تتقال كرقم رئيسي.

### ج) تجارب few-shot تانية (نفس الكود، فيديو واحد)

| التجربة | الدقة | الصدفة | عيّنات | الاختبار النظيف |
|---|---|---|---|---|
| `run_vidtest2.py` — 5 حركات | 92.3% | 20.0% | 39 | ❌ مافيش |
| `run_vidtest3.py` — 13 حركة | 16.1% | 7.7% | 484 | **0 من 50** |

⚠️ الرقمين دول **الـ template والاختبار جايين من نفس الفيديو** — متضخّمين
بالبناء. الرقم اللي يتقال للدكتور هو بتاع `run_crossvideo.py` فوق.

⚠️ **الـ 92.3% مضلّلة ولازم تتقال بالتحفّظ ده: تلات حركات بس هي اللي
اتقاست فعلاً.** `sit_down` و `stand_up` في vidtest2 طول كل واحدة **ثانية
واحدة بالظبط**، فالقصاصة بتاكل الحركة كلها ومايفضلش منها حاجة تتقاس.
اللي اتقاس: `running` (9/9) · `phone_call` (20/20) · `wave` (7/10) —
تلات حركات طويلة ومميزة. **الحركتين الصعبتين مش في الرقم أصلاً.**

في vidtest3 الاختبار النظيف الوحيد (template من ظهور الحركة الأول، اختبار
على الظهور التاني) طلّع **صفر من 50** في كل النسخ الستة اللي اتجرّبت.

### 🚨 رقم vidtest2 اتحرّك مرتين — التاني كان **بق مش نتيجة**

| الرقم | إمتى | الحقيقة |
|---|---|---|
| 83.3% | قبل تصحيح الـ GT | ✅ تفسير حقيقي — الجدول كان بيجمّع القعود والوقوف في حركة واحدة |
| **17.9%** | 2026-09-01 | ❌ **بق كاش. الرقم ده لاغي تماماً، ماكانش نتيجة أصلاً** |
| **92.3%** | 2026-09-01 (بعد الإصلاح) | ✅ الرقم الحالي، بكاش نضيف، مع التحفّظ فوق |

الـ 17.9% كانت من كاش DTW قديم اتحمّل بعدد templates مختلف، فاللابلز
اتزحلقت كلها. التفاصيل في [`fastdtw/HANDOFF.md`](fastdtw/HANDOFF.md) —
اتصلّح بـ `oneshot_core.cached_distances()`.

**الدرس:** أي رقم مكتوب في ملف markdown هو **لقطة**. لو الـ ground truth
اتغيّر أو الكود اتعدّل، شغّل السكريبت تاني قبل ما تنقل الرقم.

### د) الـ $1 Recognizer

⬜ الكود موجود (`dollar1/cell_dollar1.py`) بس لسه ماتشغّلش على أي فيديو.
فيه حاجتين مانعينه (اعتماد على متغيّرات نوتبوك الـ LSTM، و `fps=60` مكتوب
بالإيد وغلط لـ vidtest3) — التفاصيل في [`dollar1/HANDOFF.md`](dollar1/HANDOFF.md).

### هـ) الـ FastDTW نفسه — التحقق التقني (متقاس محلياً)

| الاختبار | النتيجة |
|---|---|
| نفس السلسلة مع نفسها | مسافة = 0 ✅ |
| إشارة ممطوطة ×2 مقابل الأصلية | مسافة = 0 ✅ (الـ warp شغّال) |
| الفرق عن الـ DTW الكامل عند n=30 | **0.000%** ✅ |

| n | خلايا DTW كامل | خلايا FastDTW | كسب السرعة |
|---|---|---|---|
| 30 | 900 | 391 | 1.41x |
| 120 | 14,400 | 1,799 | 6.02x |
| 480 | 230,400 | 7,527 | **24.50x** |

لما `n` تتضاعف، خلايا الـ DTW الكامل بتتربّع بينما خلايا الـ FastDTW
بتتضاعف بس. ده O(n²) مقابل O(n) بالأرقام.

عند n=30 (اللي شغالين عليه) الـ FastDTW بيدّي **نفس إجابة الـ DTW الكامل
بالظبط** — يعني مفيش أي تضحية بالدقة مقابل السرعة عند الطول ده.

---

## 3. خمس حقايق لازم تعرفها قبل ما تكمّل

**١. السقف الإحصائي = 12 ظهور حقيقي للحركة** في التلات فيديوهات كلهم.
مفيش بروتوكول بيزوّد الرقم ده. بروتوكول النوافذ بيطلّع 109 عيّنة بس دي
**مترابطة ومتقصوصة من نفس الـ 12** — ماتتحسبش 109 قياس مستقل في أي اختبار
دلالة. (مكتوبة صريح في `run_crossvideo.py:window_protocol.__doc__`.)

**٢. `sit_down` بيفشل تماماً في الـ DTW — 0% في كل نسخة.** والسبب معماري
مش معايرة: القعود والوقوف نفس مسار الجسم بالظبط والفرق **اتجاه الزمن**،
والـ DTW بتلوي الزمن فبتقدر تقلب الاتجاه. فيه دليل مصوّر: في `stand_up`
عند 32.4s الشخص مايل لقدام وإيديه على فخاده، و**نفس الوضع بالظبط** في
`sit_down` عند 12.8s.

**٣. الدقة الإجمالية لوحدها مضلّلة.** الفيديوهات معظمها سكون، فموديل بيقول
`other` على طول بياخد أرقام عالية. **أي رقم دقة لازم يتقال ومعاه خط الأساس.**

**٤. تقليل عدد الكلاسات بيضر مش بينفع.** جرّبنا 12 → 4 → 3 كلاس: دقة NTU
طلعت (97.4% → 100%) والأداء على الفيديو بقى أوحش. السبب إن الـ softmax
بيتشبّع فالثقة بتبقى بلا معنى وعتبة الرفض بتموت. **رجّعنا 10 كلاسات.**

**٥. المواضع أحسن من الفروق (velocity) مع المفردات الواسعة** — اتكرر
3 مرات. لكن الفروق أحسن في مسألة sit/stand بكلاسين. **مافيش إعداد واحد
بيكسب في كل الحالات** — ودي في حد ذاتها ملاحظة عن ضعف الطريقة.

---

## 4. الـ Ground Truth

### القاعدة الأهم في المشروع

⚠️ **متكتبش جدول ground truth بإيدك في أي ملف. استورد من `ground_truth.py` بس.**

الجدول كان متكرر في **9 أماكن**، وده اللي خلّى غلطتين حقيقيتين يعيشوا شهر:
- `vidtest3` كله كان متوصّف غلط (4 حركات بدل 13 حركة)
- الـ `wave` بتاع `vidtest2` كان متعلّم "سكون"، يعني الكشف **الصح** ليه
  كان بيتحسب إنذار كاذب

لو عايز مفردات أضيق استخدم `project()` — بيسقّط الجدول المرجعي بدل ما
تكتب جدول تاني.

### الحالة دلوقتي

| الفيديو | الحركات | الحالة |
|---|---|---|
| `vidtest1` | 4 | ✅ متحقّق منه بـ contact sheets كل 0.4s — الأربع انتقالات كلهم في حدود ±0.3s |
| `vidtest2` | 5 | ✅ متأكّد بالمشاهدة من صاحب المشروع |
| `vidtest3` | 13 | ✅ متأكّد بالمشاهدة من صاحب المشروع |
| `vidtest4` | ⬜ | ❌ **لسه مافيش ground truth خالص** — الـ keypoints متستخرجة بس |

التلات جداول عدّوا `check()` (مافيش تداخل ولا فجوات، التغطية كاملة).

⚠️ `vidtest4` **مش في `ground_truth.py`** لحد ما التوقيتات تتحط. من غيرها
`run_vidtest4.py` بيطلّع خط زمني متوقَّع بس، من غير أي رقم دقة.

⚠️ `vidtest1` لسه `verified_by_owner: False` — أنا راجعته من الصور بس
**اللي يقلبها لـ True هو صاحب المشروع لما يتفرّج**. الفيديو جاهز:
`shared/results/gt_vidtest1.mp4`.

### اصطلاح اللابلز — الفرق ده مش شكلي

| | المعنى |
|---|---|
| `'other*'` | سكون **متأكّد منه بالمشاهدة** — بيتحسب في التقييم |
| `'?'` | فترة **مش موصوفة** — بتتستثنى من الحساب تماماً |

حطّ فترة مش موصوفة على إنها `'other*'` = **اختراع بيانات**. ده اللي عمل
غلطة الـ `wave` بتاعة vidtest2.

### فيديوهات المراجعة

`render_gt.py` بيرسم الـ ground truth على الفيديو: الهيكل العظمي + اسم
الحركة في شريط فوق + شريط زمني تحت بكل الفترات ومؤشر متحرك.

```bash
python shared/render_gt.py
```

المخرجات: `shared/results/gt_vidtest{1,2,3}.mp4` (28 MB).

⚠️ دي فيديوهات **الـ ground truth** مش التوقّعات. فيديوهات التوقّعات بيطلّعها
`fastdtw/render_pred.py` في `fastdtw/results/` ومكتوب عليها بالأحمر إنها توقّع.
متخلطش بينهم — ده كان مصدر لبس قبل كده.

---

## 5. الملفات

> كل فولدر فيه `HANDOFF.md` بتاعه بجدول ملفات أتفصل. اللي هنا **خريطة
> عامة** — لو بتشتغل جوّه فولدر واحد، اقرا الـ HANDOFF بتاعه.

### `shared/` — الكود المشترك

| الملف | الدور |
|---|---|
| **`paths.py`** | ⭐ مصدر واحد للمسارات — بيشتغل محلياً وعلى Kaggle |
| **`ground_truth.py`** | ⭐ **المصدر الواحد للـ GT** — استورد من هنا |
| `gt_sources/vidtest1_ground_truth.py` | جدول vidtest1 |
| `gt_sources/vidtest2_ground_truth.py` | جدول vidtest2 |
| `gt_sources/vidtest3_ground_truth_v2.py` | جدول vidtest3 |
| `baselines_common.py` | دوال مشتركة: templates، نوافذ، تنعيم، scoring |
| `skeleton_norm.py` | دوال التطبيع (نفس كود `lstm/cells/cell3`) |
| `pose_extract.py` | استخراج الـ keypoints بـ YOLOv8n-pose |
| `render_gt.py` | يرسم الـ **GT** على الفيديوهات للمراجعة بالعين |
| `keypoints/` | النقاط المستخرجة — **متسجّلة في git** |

```bash
python shared/ground_truth.py    # الجدول الكامل + فحص السلامة
python shared/paths.py           # اتأكد إنه لاقي الفيديوهات والنقاط
```

### `lstm/` — ⭐ الطريقة الأساسية

| الملف | الدور |
|---|---|
| `cells/cell2..cell10*.py` | 9 خلايا: الكلاسات · التطبيع · التقسيم · الموديل · التقييم · الفيديو · التنعيم · الخط الزمني |
| `lstm_ntu60.ipynb` | النوتبوك **المولّد** — متعدّلوش بالإيد |
| `apply_edits.py` | يحقن `cells/*.py` في النوتبوك |

### `fastdtw/` — خط الأساس اللي الدكتور طلبه

| الملف | الدور |
|---|---|
| `fastdtw_core.py` | **تنفيذ FastDTW** (Salvador & Chan 2007) — numpy خالص |
| `oneshot_core.py` | النواة: تمثيل · مسافات · **كاش** · عتبات · hubness |
| **`run_crossvideo.py`** | ⭐ الاختبار النظيف — template من فيديو، اختبار على تاني |
| `run_vidtest2.py` · `run_vidtest3.py` · `run_vidtest4.py` | تجارب فيديو واحد |
| `run_oneshot.py` | leave-one-video-out + معايرة |
| `demo_oneshot.py` | شرح خطوة بخطوة + رسم |
| `cell_fastdtw_ntu.py` | ⬜ خلية نوتبوك — FastDTW **بالاعتماد على NTU** |
| `render_pred.py` | يرسم **التوقّعات** على الفيديو (مش الـ GT) |

### `dollar1/` — خط الأساس التالت

| الملف | الدور |
|---|---|
| `cell_dollar1.py` | الـ $1 Recognizer ⬜ **لسه ماتشغّلش** — شوف `dollar1/HANDOFF.md` |

### في الجذر

| الملف | الدور |
|---|---|
| `fetch_results.py` | يسحب مخرجات الرن من Kaggle |
| `kernel-metadata.json` | إعدادات كيرنل Kaggle (`code_file` -> `lstm/lstm_ntu60.ipynb`) |

### الأسامي اتغيّرت 2026-09-01

| قديم (مسطّح) | جديد |
|---|---|
| `notebook520b8d324a.ipynb` | `lstm/lstm_ntu60.ipynb` |
| `cell*.py` | `lstm/cells/cell*.py` |
| `notebook_fastdtw.py` | `fastdtw/cell_fastdtw_ntu.py` |
| `notebook_oneshot_fastdtw.py` | `fastdtw/demo_oneshot.py` |
| `notebook_dollar1.py` | `dollar1/cell_dollar1.py` |
| `vidtest*_ground_truth*.py` | `shared/gt_sources/` |

**الاصطلاح الجديد:** `cell_*` = خلية نوتبوك (محتاجة متغيّرات من خلايا قبلها،
**مش** بتشتغل لوحدها) · `run_*` = تجربة بتشتغل لوحدها · `demo_*` = شرح.

⚠️ كل سكريبت جوّه فولدر بيبدأ بـ `import _bootstrap` **قبل** أي استيراد من
`shared/`. السطر ده هو اللي بيحط `shared/` في `sys.path` — لو اتشال أو
اتنقل تحت، الاستيراد بيقع.

---

## 5.5 المسارات — ليه فيه ملف مخصوص ليها

قبل 2026-09-01 مسار `keypoints/` كان مكتوب بالإيد في **6 ملفات**، وكل واحد
كاتب دالة `load()` بتاعته بنفس السطرين. ده كان معناه:

* أي نقلة لـ Kaggle محتاجة 6 تعديلات، وأي واحد يتنسي = بق
* السكريبتات مكانش ينفع تتشغّل إلا من جوّه فولدر المشروع (مسارات نسبية)

دلوقتي كله بييجي من `shared/paths.py`:

```python
import _bootstrap  # noqa: F401   ← لازم يبقى **قبل** السطر اللي بعده
from paths import load_keypoints, KP_DIR, VIDEO_DIR, CACHE_DIR, out_dir

kp, fps = load_keypoints('vidtest4')      # بيرجّع (keypoints, effective_fps)
OUT_DIR = out_dir(__file__)               # results/ جنب السكريبت نفسه
```

| | محلي | Kaggle |
|---|---|---|
| `KP_DIR` (قراءة) | `shared/keypoints/` | `shared/keypoints/` اللي جات مع الـ clone |
| `VIDEO_DIR` | `testvid_upload/` | `/kaggle/input/testvid/` |
| `KP_OUT` (كتابة) | نفس `KP_DIR` | `/kaggle/working/keypoints/` |
| `out_dir(__file__)` | `<فولدر السكريبت>/results/` | نفسه، أو `/kaggle/working/results/` لو للقراءة بس |
| `CACHE_DIR` | `_scratch/dtw_cache/` | `/kaggle/working/_scratch/dtw_cache/` |

⚠️ **`OUT_DIR` بقى دالة مش ثابت.** كان `OUT_DIR` واحد للمشروع كله، ودي كانت
مشكلة: تلات طرق بيكتبوا في نفس الفولدر وحد يبص على ملف مايعرفش مين طلّعه.
دلوقتي `out_dir(__file__)` بيرجّع `results/` اللي **جنب السكريبت**، فنتايج
الـ LSTM في `lstm/results/` والـ FastDTW في `fastdtw/results/`.

**ترتيب البحث عن الـ keypoints** (أول واحد موجود وفيه ملفات بيكسب):

```
١. متغيّر البيئة KEYPOINTS_DIR      ← للتجارب، بيكسر كل اللي تحته
٢. keypoints/ اللي جنب الكود         ← بتيجي مع الـ git clone
٣. /kaggle/input/*/keypoints/        ← الداتاسِت المرفوع
٤. /kaggle/working/keypoints/        ← لو اتولدت في نفس الجلسة
```

⚠️ **رقم ٢ قبل رقم ٣ بنيّة، ودي كانت بق فعلي.** أول نسخة من `paths.py` كانت
بتروح على الداتاسِت على طول وبتتجاهل الـ `keypoints/` اللي قدامها. النتيجة
إن الـ `git clone` على Kaggle كان هيدوّر في فولدر فاضي رغم إن الملفات
معاه في الريبو. دلوقتي اللي جنب الكود له الأولوية — وده اللي بيخلّي المسار
يشتغل **من غير أي داتاسِت**.

⚠️ الفرق بين `KP_DIR` و `KP_OUT` مش تفصيلة: على Kaggle الـ `input` **للقراءة
بس**. `pose_extract.py` بيكتب فبيستخدم `KP_OUT`؛ باقي السكريبتات بتقرا
فبتستخدم `KP_DIR`.

الاكتشاف أوتوماتيك (`Path('/kaggle/input').is_dir()`)، ومحدش محتاج يعدّل
حاجة. للتجارب تقدر تكسر أي مسار بمتغيّر بيئة:

```bash
KEYPOINTS_DIR=/some/where python fastdtw/run_vidtest4.py
```

`python shared/paths.py` لوحده بيطبّع تشخيص: المسارات، ومنين جابت الـ keypoints
(الريبو ولا الداتاسِت)، وليستة الفيديوهات اللي لقاها. شغّله أول حاجة لو
حاجة استغربتها.

**اتأكدنا إزاي إنها شغالة؟** اتّعملت محاكاتين:
1. فولدر بنفس بنية `/kaggle/input` بالظبط
2. `git clone` نضيف في مكان تاني، من غير داتاسِت ولا mp4 ولا كاش

الاتنين طلّعوا نفس النتيجة الحرف بالحرف.

`keypoints/` اتنسخت كمان جوّه `testvid_upload/` (828 KB) عشان تترفع مع
الداتاسِت — مش لازمة لمسار الـ clone، بس بتنفع لو حد شغّل من الداتاسِت.

---

## 6. اللي لسه ناقص

```
☐ 1. شغّل الـ LSTM على vidtest2 و vidtest3 (على الـ ground truth الصح)
☐ 2. شغّل FastDTW (مسار NTU) على التلات فيديوهات
☐ 3. شغّل الـ $1 Recognizer على التلات فيديوهات
☐ 4. اعمل الجدول النهائي للمقارنة
☐ 5. صاحب المشروع يتفرّج على shared/results/gt_vidtest1.mp4 ويأكّد الجدول
☐ 6. صاحب المشروع يدّي توقيتات vidtest4 → تتحط في ground_truth.py
     (من غيرها fastdtw/run_vidtest4.py بيطلّع خط زمني بس، من غير رقم)
```

### 🐛 درس من بق اتصلّح في `run_vidtest4.py` (2026-09-01)

الكود كان بيقرا `meta.get('detection_rate', 0)` والمفتاح الحقيقي اسمه
`detect_rate`. النتيجة: التقرير كان بيطبع **"اكتشاف 0.0%"** بدل 93.9%.

`pose_extract.py` كان سليم — البج كان في القراءة مش في الكتابة.

**الدرس العام:** `.get(key, default)` على مفتاح **المفروض** يبقى موجود
بيحوّل غلطة إملائية لرقم كاذب في التقرير بدل ما يقع. لما المفتاح لازم
يكون موجود، استخدم الأقواس المربعة `meta['detect_rate']` عشان يقع بصوت عالي.

**تشغيل الـ baselines على Kaggle:**

```python
# جوّه lstm_ntu60.ipynb، بعد ما الخلايا اللي فوق تشتغل
# (عشان X_train_sk و all_keypoints يبقوا متحملين)
exec(open('../fastdtw/cell_fastdtw_ntu.py').read())
exec(open('../dollar1/cell_dollar1.py').read())
```

عشان تغيّر الفيديو، عدّل `VIDEO_NAME` في أول `cell_fastdtw_ntu.py`. الـ fps
بيتظبط لوحده.

**تشغيل مسار الـ few-shot محلياً** (مش محتاج Kaggle ولا NTU):

```bash
python shared/pose_extract.py && python fastdtw/run_crossvideo.py
```

**تشغيل مسار الـ few-shot على Kaggle** — نفس الأوامر بالظبط، من غير أي تعديل:

```python
!git clone -q -b crossvideo-and-gt-unification https://github.com/gradcs2027/Test1.git
%cd Test1/kaggle_nb_520b8d324a
!python shared/paths.py            # بيطبع المسارات اللي لقاها — بصّ عليها الأول
!python fastdtw/run_crossvideo.py  # ⭐ الرقم النضيف
!python fastdtw/run_vidtest4.py    # التنبؤ الأعمى
```

⚠️ السكريبتات بتشتغل من **أي مكان** (`_bootstrap` بيتصرّف في الـ `sys.path`)،
فمش لازم تعمل `cd fastdtw` — بس لو عملت، الأمر يبقى `python run_crossvideo.py`.

⚠️ **الـ `-b` مش اختياري** لحد ما الفرع يتدمج في `main`. `main` لسه 6 commits
وراه ومافيهاش `paths.py` ولا `run_vidtest4.py`. التفاصيل في `KAGGLE_RUN.md`.

#### تلات حاجات مش محتاجينها، وناس بتفتكر إننا محتاجينها

| | ليه لأ |
|---|---|
| **`pip install`** | `fastdtw_core.py` تطبيق **محلي** مكتوب في الريبو، مش حزمة pip. مسار الـ few-shot كله معتمد على **numpy بس**، و Kaggle فيها numpy أصلاً |
| **الداتاسِت** | التصنيف شغّال على الـ keypoints (828 KB، متسجّلة في git فبتيجي مع الـ clone)، مش على ملفات الـ mp4. الداتاسِت محتاجينه بس لـ `pose_extract.py` و `render_*.py` |
| **الـ GPU** | كله numpy على CPU. `run_crossvideo` 6 ثواني، `run_vidtest4` 29 ثانية، `run_vidtest3` 43 ثانية |

**اتأكدنا إزاي؟** اتعمل `git clone` نضيف في مكان تاني خالص، من غير داتاسِت
ولا ملف mp4 ولا كاش، واتشغّل عليه `run_vidtest4.py` — حسب الـ 30,567 مسافة
DTW من الأول وطلّع **نفس النتيجة بالحرف**.

⚠️ لو `python shared/paths.py` طلّع **صفر فيديوهات**، الـ `KP_DIR` اللي بيطبعه هو
اللي دوّر فيه — قارنه بمكان الملفات. مش هيكمّل ويطلّع أرقام غلط، هيقع بصوت عالي.

---

## 7. تفاصيل تقنية سريعة

- **`vidtest3` بـ 30fps، و `vidtest1`/`vidtest2` بـ 60fps.** خلي بالك في أي
  حساب بالثواني.
- `FRAME_SKIP = 2` في الاستخراج — يعني رقم الـ keypoint = رقم فريم الفيديو ÷ 2.
- **إعدادات الـ inference:** `CONF_REJECT = 0.60` · moving average بـ `k=5` ·
  `MIN_SEGMENT = 2` · نوافذ 1.5/2/3/4 ثانية + mirror TTA · معايرة حرارية
  بـ grid search من 0.5 لـ 3.0 (الحالية 1.65).
- **التطبيع لازم يكون واحد في التدريب والفيديو** — أي اختلاف بيكسّر النتيجة.
- **الـ OpenCV بتاع pip مافيهوش H.264 شغّال.** لو هتكتب فيديو استخدم
  `imageio-ffmpeg` زي `render_gt.py`، وخلي الأبعاد **زوجية** (`libx264` +
  `yuv420p` بيفشل على أي بُعد فردي).

---

## 8. TIER 2 — تحسين بنك الـ templates (آخر شغل)

**الملف:** `fastdtw/evaluate_crossvideo_tier2.py`
**النتائج المشروحة:** `fastdtw/results/tier2_results_annotated.txt`

### ليه عملناه
TIER 1 (تظبيط `RADIUS` + عتبة ثقة) فشل تماماً — كل قيم RADIUS من ١ لـ ٥
طلّعت نفس الرقم بالحرف. السبب من `debug_confidence.py`: المسافات متقاربة
جداً (1.21–1.42)، فترتيب الـ `argmin` مابيتغيّرش. يعني المشكلة **مش** في
حساب المسافة، المشكلة في **hubness**: templates بتقع في نُص فضاء الملامح
فبتطلع أقرب واحد لأي حاجة. `brush_hair` لوحده بلع ٨٩ نافذة غلط.

### التلات خطوات
| # | الخطوة | الفكرة |
|---|--------|--------|
| ١ | **معايرة z لكل template** | `z = (d − μ_t) / σ_t` — μ و σ متحسبين من قصاصات مرجعية من المصدر. السؤال بقى "قريبة منه بالنسبة لعادته؟" مش "قريبة منه مطلقاً؟". ده اللي شال ميزة الـ hubs الوهمية |
| ٢ | **تصويت أقرب ٣ جيران** | بوزن `1/(الترتيب+1)` بدل الاعتماد على template واحد ممكن يكون شاذ |
| ٣ | **تنضيف البنك** | عدّ النفع/الضرر لكل template على المصدر، وشطب اللي `good==0 and bad>=2` |

### مافيش تسريب
كل حاجة بتتعلّم (μ، σ، عدّاد النفع/الضرر، قرار الشطب) بتتحسب من
**فيديوهات المصدر بس**. وكمان الـ template الجاي من فيديو `s` بيتعاير
باستثناء القصاصات المرجعية اللي من `s` نفسه.

### النتائج (n=29، ٨ حركات مشتركة)
| الطريقة | قصاصات | نوافذ |
|---------|--------|-------|
| الأساس 1-NN | 13.8% (4/29) | 17.4% (61/350) |
| + معايرة z | **24.1% (7/29)** | 25.4% |
| + تصويت ٣ جيران | 24.1% | 25.4% |
| + تنضيف البنك ⭐ | 24.1% | **25.7% (90/350)** |

- خط أساس الأغلبية: **20.7%** (قصاصات) / 21.7% (نوافذ) · الصدفة 12.5%
- **p = 0.393** → فوق خط الأساس بـ 3.4 نقطة بس، لكن **مش دال إحصائياً**

### الحكم الصريح
✅ الشغل نفسه نجح: +10.3 نقطة، والمعايرة (خطوة ١) هي اللي جابت كل المكسب
تقريباً — دي أول مرة نتخطى خط أساس الأغلبية أصلاً.
❌ لكن **ماينفعش نقول إن الطريقة نجحت إحصائياً**. بـ 29 عيّنة، الفرق
مايتفرقش عن الحظ.

`phone_call` (90.7%) و `clapping` (69.4%) بقوا كويسين في بروتوكول النوافذ.
`wave` (1.3%) و `walking` (0%) لسه فاشلين تماماً.

### 🔬 السقف الحقيقي
**29 ظهور** في ٤ فيديوهات. ده سقف الإحصاء مش سقف الخوارزمية — مهما
حسّنّا، مافيش أي تحسين هيبقى دال إحصائياً على العدد ده. **الحل الوحيد
فيديوهات أكتر**، مش كود أكتر.

---

## 9. TIER 3 — تحسين مسافة الـ DTW نفسها (نتيجة سلبية)

**الملف:** `fastdtw/evaluate_crossvideo_tier3.py`
**النتائج المشروحة:** `fastdtw/results/tier3_results_annotated.txt`

### الفكرة
TIER 2 صلّح **إزاي بنقارن** المسافات. TIER 3 حاول يصلّح **إزاي بنحسبها**.

التشخيص اللي بنى عليه: كل الالتباسات بعد TIER 2 كانت **جوّه نفس مجموعة
الجسم** — حركات الدراع (`wave`/`clapping`/`spray_perfume`/`brush_hair`/
`phone_call`) بتتلخبط في بعض، وحركات الرجل (`walking`/`sitting`/
`stand_up`) في بعض. يعني الـ DTW شايف "الجسم اتحرك" بس مش شايف **مين**.

### التلات خطوات
| # | الخطوة | النتيجة |
|---|--------|---------|
| أ | **DTW كامل بدل تقريب FastDTW** — المتتالية 30 فريم = 841 خانة بس، فالحساب الدقيق رخيص | **صفر فرق** (24.1% → 24.1%) |
| ب | **شريط Sakoe-Chiba ±4** — يمنع المسار يبعد عن القُطر فماتقدرش حركة "تتلوى" لحد ما تشبه حركة تانية | ▼ 20.7% |
| ج | **أوزان مفاصل متعلَّمة بنسبة فيشر** (تباين بين الحركات ÷ تباين داخلها، مخلوطة بالمتساوي بـ ALPHA=0.5) | ▼▼ 13.8% |

### النتائج
| النسخة | قصاصات (n=29) | نوافذ (n=350 مترابطة) |
|--------|---------------|------------------------|
| TIER 2 | **24.1% (7/29)** | 25.7% |
| أ. DTW كامل | 24.1% (7/29) | 26.0% |
| ب. + شريط | 20.7% (6/29) | **27.7%** |
| ج. + أوزان | 13.8% (4/29) | 27.1% |

خط أساس الأغلبية 20.7% (قصاصات) / 21.7% (نوافذ) · **p = 0.878** لـ TIER 3

### الحكم الصريح
❌ **TIER 3 فشل.** رجّعنا لتحت خط أساس الأغلبية. **TIER 2 لسه أحسن حاجة عندنا.**

تلات ملاحظات مهمة:
1. **تقريب FastDTW مش هو المشكلة** — الخطوة (أ) طلّعت صفر فرق بالظبط.
   يعني كل الكلام على "التقريب بياكل من الدقة" كان غلط.
2. **أوزان المفاصل ضرّت** رغم إن التشخيص كان منطقي. الأوزان اتعلّمت من
   ٢٩ عيّنة فاتشكّلت من الضوضاء (رسغ شمال أخد 1.69، كوع شمال 0.71 —
   مافيش سبب تشريحي للفرق ده).
3. **البروتوكولان اتناقضوا**: الشريط ضرّ القصاصات وحسّن النوافذ. على
   ٢٩ عيّنة، ده معناه إن الفروق دي **ضوضاء مش إشارة**.

### 🔬 الاستنتاج بعد التلات مستويات كلها
المكسب الحقيقي الوحيد جه من **معايرة الـ hubness** (TIER 2 خطوة ١):
13.8% → 24.1%. كل حاجة تانية — RADIUS، عتبات ثقة، DTW كامل، شرايط،
أوزان — مافرقتش أو ضرّت.

**السقف مش الخوارزمية، ده عدد العيّنات.** 29 ظهور في 4 فيديوهات.
حتى لو TIER 4 طلّع 40%، هيفضل `p > 0.05`.
**الخطوة الجاية اللي هتفرق مش كود — فيديوهات أكتر.**

---

## 10. فيديوهات التوقّعات (TIER 2)

**الملف:** `fastdtw/render_tier2_pred.py`
**الخرج:** `fastdtw/results/tier2_vidtest{1,2,3,4}.mp4`

```bash
python fastdtw/render_tier2_pred.py            # الأربعة
python fastdtw/render_tier2_pred.py vidtest1   # واحد بس
```

### شكل الفيديو
| الجزء | المحتوى |
|-------|---------|
| فوق | `TRUE` (الحركة الصح) + `PRED` (توقّع TIER 2) + `CORRECT`/`WRONG` + دقة الفيديو ده |
| النُص | الفيديو + الهيكل العظمي مرسوم |
| شريط ١ | `GROUND TRUTH` — الإجابة الصح |
| شريط ٢ | `FASTDTW TIER 2 PREDICTION` — توقّع الموديل |

نفس اللون لنفس الحركة في الشريطين عشان المقارنة بالعين تبقى سهلة.
الفترات **الغامقة** = مش داخلة في الحساب (`?` أو حركة مش مشتركة).

### النتايج لكل فيديو
| الفيديو | النتيجة |
|---------|---------|
| vidtest1 | 2/7 (28.6%) |
| vidtest2 | 1/3 (33.3%) |
| vidtest3 | 2/9 (22.2%) |
| vidtest4 | 2/10 (20.0%) |
| **الإجمالي** | **7/29 = 24.1%** ← نفس رقم التقرير بالحرف |

### ⚠️ ليه على فترات الحركة بس مش الفيديو كله؟
الفيديو كله محتاج **عتبة رفض** تقرر إمتى الشخص ساكن — والعتبة دي معامل
بيتظبط، وده الفخ اللي وقعنا فيه قبل كده (قسم 6.7: "علّي الـ accuracy فخ").
فبنرسم **اللي قِسناه بالظبط** — بروتوكول القصاصات اللي طلّع الـ 24.1%.

### 🐛 `render_pred.py` القديم مكسور
بيعمل `from run_vidtest4 import predict, segments` — والملف ده اتغيّر اسمه
لـ `evaluate_vidtest4.py`، فبيطلّع `ModuleNotFoundError`. وكمان كان
بيستخدم الخوارزمية القديمة وقبل تصحيح الـ ground truth.
الملفات القديمة `fastdtw/results/pred_vidtest*.mp4` **قديمة ومضلّلة** —
استخدم `tier2_*.mp4` بدلها.

---

## ١١. نوتبوك Kaggle — كل الكود مكتوب جوّه

**الملف:** `fastdtw/fastdtw_kaggle_full.ipynb` (31 خلية: 16 كود + 15 شرح)

نوتبوك واحد قايم بذاته فيه **كل** اللي اتعمل، والكود **مكتوب جوّه
الخلايا** — مافيش أي `!python file.py` ولا `import` من ملفات المشروع.
يترفع على Kaggle ويشتغل من غير أي حاجة تانية.

**اللي جوّه:**

| الخطوة | المحتوى |
|--------|---------|
| ١ | تحميل الـ keypoints — `find_keypoints_dir()` بتدوّر أوتوماتيك على `/kaggle/input` |
| ٢ | الـ ground truth للأربع فيديوهات **مكتوب حرفياً** + `spans` / `shared_labels` |
| ٣ | FastDTW من الصفر: `_dtw_windowed`, `_reduce_by_half`, `_expand_window`, `fastdtw` + عدّاد خلايا يثبت O(n) |
| ٤ | `normalize_window` (مرساة واحدة), `to_features`, `resample_linear`, `norm_distance` |
| ٥ | `cut_templates`, `balance_templates`, `build_templates` |
| ٦ | الأساس 1-NN — بروتوكولي القصاصات والنوافذ |
| ٧ | TIER 1 — RADIUS من ١ لـ ٥ + تشخيص المسافات المتقاربة |
| ٨ | تشخيص الـ hubness (مين بيبلع مين) |
| ٩ | TIER 2 — `calibrate`, `predict_t2`, سلّم الطرق الأربعة |
| ١٠ | TIER 3 — `dtw_banded`, `learn_joint_weights` + عرض الأوزان |
| ١١ | التقرير النهائي -> `fastdtw_results_annotated.txt` |
| ١٢ | تعليمات التحميل (Kaggle / Colab) |

**الملف المولّد:** `_scratch/build_kaggle_notebook.py` — النوتبوك بيتبني
منه، مش بالإيد. لو عايز تعدّل، عدّل المولّد وشغّله تاني:

```bash
python _scratch/build_kaggle_notebook.py
```

**⚠️ اتأكد إنه شغّال:** استخرجت خلايا الكود وشغّلتها كاملة على الداتا
الحقيقية. النوتبوك بيطلّع **نفس أرقام التقارير بالحرف**:

| المرحلة | الدقة (قصاصات) | p |
|---------|----------------|---|
| الأساس 1-NN | 13.8% (4/29) | 0.878 |
| TIER 2 ⭐ | **24.1%** (7/29) | 0.393 |
| TIER 3 | 13.8% (4/29) | 0.878 |

**تصحيحان اتعملوا وقت التحقق** (الكلام كان مش مطابق للأرقام):
1. كنت كاتب "كل قيم RADIUS طلّعت نفس الرقم بالحرف" — الحقيقة ٤ من ٥
   طلّعوا نفس الرقم و radius=3 طلّع **أوحش** (10.3%). النص بقى محسوب
   من الأرقام نفسها بدل ما يكون مكتوب ثابت.
2. جدول عدّ الخلايا في الشرح كان أرقامه قديمة (1.41× عند n=30) والقياس
   الفعلي 2.30×. شلت الجدول الثابت وسبت الخلية تطبع القياس الحقيقي.
