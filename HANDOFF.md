# HANDOFF — الحالة دلوقتي

> الملف ده بيوصف **الوضع الحالي**: ايه الشغّال، ايه الأرقام المتقاسة فعلاً،
> وايه اللي لسه ناقص. مش تاريخ ولا لوج.
>
> للشرح التفصيلي وأسئلة المناقشة: `README.md`
> **آخر تحديث:** 2026-09-01

---

## 1. المشروع في سطرين

التعرّف على حركة الإنسان من الهيكل العظمي:

```
فيديو ──► YOLOv8n-pose ──► 17 نقطة لكل فريم ──► BiLSTM ──► اسم الحركة
```

**الهدف من الشغل الحالي:** نثبت إن الـ LSTM المتدرّبة أحسن من الطرق الأبسط
(DTW / FastDTW / $1 Recognizer) على نفس الفيديوهات.

الفيديوهات: `vidtest1` (37.5s) · `vidtest2` (28.5s) · `vidtest3` (126.5s).

---

## 2. الأرقام

⬜ = لسه ماتشغّلش. **مافيش رقم مخمّن أو متوقّع في الملف ده** — الموجود متقاس بس.

### أ) الـ LSTM — متدرّبة على NTU-60 (56 ألف عينة)

| | vidtest1 | vidtest2 | vidtest3 |
|---|---|---|---|
| الدقة | **71.8%** | ⬜ | ⬜ |
| خط الأساس (`other` للكل) | 35.4% | | |
| real recall | 72.6% | | |
| other recall | 70.4% | | |

7 حركات من 8 اتعرفت بالكلاس الصح. الغلط الوحيد: `rub_hands` اتقالت `clap`.

### ب) الـ FastDTW من غير أي داتاسِت — **أنضف رقم عندنا**

الفكرة: بدل ما نتدرّب على NTU، بناخد قصاصة من حركة في فيديو ونستخدمها
`template`، وبنقيس المسافة بينها وبين باقي الفيديوهات.

`run_crossvideo.py` — **template من فيديو، اختبار على فيديو تاني**
(كاميرا تانية، لبس تاني، يوم تاني = مافيش أي تسريب):

| | الدقة | خط أساس الأغلبية | |
|---|---|---|---|
| **عبر-الفيديوهات** | **41.7%** (5 من 12) | 41.7% | ❌ صفر تحسّن |
| جوّه الفيديو الواحد (للمقارنة) | 75.0% (6 من 8) | 50.0% | ✅ |

**الرقم اللي يتقال للدكتور هو 41.7%.**

- 41.7% دي **بالظبط** خط أساس الأغلبية — يعني "قول `stand_up` على طول"
  بيدّي نفس النتيجة. مافيش مهارة متعلّمة.
- الفرق **+33.3 نقطة** بين "جوّه الفيديو" و"عبر الفيديوهات" هو نفس الكود
  بالظبط والفرق الوحيد مصدر الـ template. يعني أي رقم قديم اتقاس جوّه
  الفيديو الواحد كان متضخّم بحوالي 33 نقطة.
- أحسن نسخة (مواضع بدل فروق) بتدّي 58.3%، **بس مش دالة إحصائياً**
  (p = 0.19 على 12 عيّنة) — ومختارة **بعد** ما شفنا النتيجة، فمينفعش
  تتقال كرقم رئيسي.

### ج) تجارب few-shot تانية (نفس الكود، فيديو واحد)

| التجربة | الدقة | أحسن نسخة | الصدفة | الاختبار النظيف |
|---|---|---|---|---|
| `run_vidtest2.py` — 5 حركات | **17.9%** | 33.3% | 20.0% | ❌ مافيش |
| `run_vidtest3.py` — 13 حركة | 16.1% | 27.7% | 7.7% | **0 من 50** |

⚠️ الرقمين دول **الـ template والاختبار جايين من نفس الفيديو**، فالمفروض
يكونوا متضخّمين — ومع ذلك vidtest2 **تحت الصدفة**.

في vidtest3 الاختبار النظيف الوحيد (template من ظهور الحركة الأول، اختبار
على الظهور التاني) طلّع **صفر من 50** في كل النسخ الستة اللي اتجرّبت.

🚨 **رقم vidtest2 اتغيّر من 83.3% لـ 17.9% ومعاه سبب واضح.** الـ 83.3%
القديمة كانت متقاسة والجدول لسه بيعتبر القعود والوقوف **حركة واحدة**
(`sit_down_stand_up`). لما الجدول اتصحّح وبقوا حركتين منفصلتين، بقى فيه
5 كلاسات بدل 4، وأهم من كده: كل واحدة منهم طولها ثانية واحدة بالظبط
فبتتاكل كلها كـ template ومايفضلش منها حاجة تتقاس. الـ `phone_call`
(20 عيّنة) بقت بتتلبّس بيهم فنزلت من 100% لـ 0%.

الخلاصة: **متستخدمش رقم vidtest2 القديم.** والدرس إن الرقم كان معتمد
على تجميع حركتين مع بعض، وده بالظبط الحاجة اللي الـ DTW مش قادرة تفرّق
فيها (حقيقة رقم ٢ تحت).

### د) الـ $1 Recognizer

⬜ الكود موجود (`notebook_dollar1.py`) بس لسه ماتشغّلش على أي فيديو.

### هـ) الـ FastDTW نفسه — التحقق التقني (متقاس محلياً)

| الاختبار | النتيجة |
|---|---|
| نفس السلسلة مع نفسها | مسافة = 0 ✅ |
| إشارة ممطوطة ×2 مقابل الأصلية | مسافة = 0 ✅ (الـ warp شغّال) |
| الفرق عن الـ DTW الكامل عند n=30 | **0.000%** ✅ |

| n | خلايا DTW كامل | خلايا FastDTW | كسب السرعة |
|---|---|---|---|
| 30 | 900 | 391 | 1.41x |
| 120 | 14,400 | 1,799 | 6.02x |
| 480 | 230,400 | 7,527 | **24.50x** |

لما `n` تتضاعف، خلايا الـ DTW الكامل بتتربّع بينما خلايا الـ FastDTW
بتتضاعف بس. ده O(n²) مقابل O(n) بالأرقام.

عند n=30 (اللي شغالين عليه) الـ FastDTW بيدّي **نفس إجابة الـ DTW الكامل
بالظبط** — يعني مفيش أي تضحية بالدقة مقابل السرعة عند الطول ده.

---

## 3. خمس حقايق لازم تعرفها قبل ما تكمّل

**١. السقف الإحصائي = 12 ظهور حقيقي للحركة** في التلات فيديوهات كلهم.
مفيش بروتوكول بيزوّد الرقم ده. بروتوكول النوافذ بيطلّع 109 عيّنة بس دي
**مترابطة ومتقصوصة من نفس الـ 12** — ماتتحسبش 109 قياس مستقل في أي اختبار
دلالة. (مكتوبة صريح في `run_crossvideo.py:window_protocol.__doc__`.)

**٢. `sit_down` بيفشل تماماً في الـ DTW — 0% في كل نسخة.** والسبب معماري
مش معايرة: القعود والوقوف نفس مسار الجسم بالظبط والفرق **اتجاه الزمن**،
والـ DTW بتلوي الزمن فبتقدر تقلب الاتجاه. فيه دليل مصوّر: في `stand_up`
عند 32.4s الشخص مايل لقدام وإيديه على فخاده، و**نفس الوضع بالظبط** في
`sit_down` عند 12.8s.

**٣. الدقة الإجمالية لوحدها مضلّلة.** الفيديوهات معظمها سكون، فموديل بيقول
`other` على طول بياخد أرقام عالية. **أي رقم دقة لازم يتقال ومعاه خط الأساس.**

**٤. تقليل عدد الكلاسات بيضر مش بينفع.** جرّبنا 12 → 4 → 3 كلاس: دقة NTU
طلعت (97.4% → 100%) والأداء على الفيديو بقى أوحش. السبب إن الـ softmax
بيتشبّع فالثقة بتبقى بلا معنى وعتبة الرفض بتموت. **رجّعنا 10 كلاسات.**

**٥. المواضع أحسن من الفروق (velocity) مع المفردات الواسعة** — اتكرر
3 مرات. لكن الفروق أحسن في مسألة sit/stand بكلاسين. **مافيش إعداد واحد
بيكسب في كل الحالات** — ودي في حد ذاتها ملاحظة عن ضعف الطريقة.

---

## 4. الـ Ground Truth

### القاعدة الأهم في المشروع

⚠️ **متكتبش جدول ground truth بإيدك في أي ملف. استورد من `ground_truth.py` بس.**

الجدول كان متكرر في **9 أماكن**، وده اللي خلّى غلطتين حقيقيتين يعيشوا شهر:
- `vidtest3` كله كان متوصّف غلط (4 حركات بدل 13 حركة)
- الـ `wave` بتاع `vidtest2` كان متعلّم "سكون"، يعني الكشف **الصح** ليه
  كان بيتحسب إنذار كاذب

لو عايز مفردات أضيق استخدم `project()` — بيسقّط الجدول المرجعي بدل ما
تكتب جدول تاني.

### الحالة دلوقتي

| الفيديو | الحركات | الحالة |
|---|---|---|
| `vidtest1` | 4 | ✅ متحقّق منه بـ contact sheets كل 0.4s — الأربع انتقالات كلهم في حدود ±0.3s |
| `vidtest2` | 5 | ✅ متأكّد بالمشاهدة من صاحب المشروع |
| `vidtest3` | 13 | ✅ متأكّد بالمشاهدة من صاحب المشروع |

التلات جداول عدّوا `check()` (مافيش تداخل ولا فجوات، التغطية كاملة).

⚠️ `vidtest1` لسه `verified_by_owner: False` — أنا راجعته من الصور بس
**اللي يقلبها لـ True هو صاحب المشروع لما يتفرّج**. الفيديو جاهز:
`results/gt_vidtest1.mp4`.

### اصطلاح اللابلز — الفرق ده مش شكلي

| | المعنى |
|---|---|
| `'other*'` | سكون **متأكّد منه بالمشاهدة** — بيتحسب في التقييم |
| `'?'` | فترة **مش موصوفة** — بتتستثنى من الحساب تماماً |

حطّ فترة مش موصوفة على إنها `'other*'` = **اختراع بيانات**. ده اللي عمل
غلطة الـ `wave` بتاعة vidtest2.

### فيديوهات المراجعة

`render_gt.py` بيرسم الـ ground truth على الفيديو: الهيكل العظمي + اسم
الحركة في شريط فوق + شريط زمني تحت بكل الفترات ومؤشر متحرك.

```bash
python render_gt.py
```

المخرجات: `results/gt_vidtest{1,2,3}.mp4` (إجمالي `results/` = 39 MB).

---

## 5. الملفات

### الـ pipeline بتاع الـ LSTM

الـ `.py` هي **المصدر الوحيد للحقيقة**، والـ `.ipynb` مولّد منها بـ
`apply_edits.py`. **متعدّلش الـ notebook بالإيد.**

| الملف | الدور |
|---|---|
| `cell2_other_class.py` | اختيار الكلاسات العشرة |
| `cell3_normalization.py` | تمركز على وسط الحوض + قياس بطول الجذع + 30 فريم |
| `cell4_split.py` | تقسيم Cross-Subject + augmentation |
| `cell5_model.py` | BiLSTM ‏256→128→128 |
| `cell6_eval.py` | التقييم على NTU |
| `cell7_video.py` · `cell8_extract.py` | تحميل الفيديو + استخراج النقاط |
| `cell9_smoothing.py` | نوافذ متعددة + mirror TTA + معايرة + رفض |
| `cell10_timeline.py` | الخط الزمني + القياس |
| `apply_edits.py` | يحقن الـ cells في النوتبوك |
| `fetch_results.py` | يسحب مخرجات الرن من Kaggle |

### الـ Ground Truth

| الملف | الدور |
|---|---|
| **`ground_truth.py`** | ⭐ **المصدر الواحد** — استورد من هنا |
| `vidtest1_ground_truth.py` | جدول vidtest1 |
| `vidtest2_ground_truth.py` | جدول vidtest2 |
| `vidtest3_ground_truth_v2.py` | جدول vidtest3 |
| ~~`vidtest3_ground_truth.py`~~ | 🚫 متقاعد — بياناته غلط. بيرمي `ImportError` |
| `render_gt.py` | يرسم الـ GT على الفيديوهات للمراجعة بالعين |

```bash
python ground_truth.py    # الجدول الكامل + فحص السلامة + الحركات المشتركة
```

### الـ baselines

| الملف | الدور |
|---|---|
| `fastdtw_core.py` | **تنفيذ FastDTW** (Salvador & Chan 2007) |
| `baselines_common.py` | دوال مشتركة: templates، نوافذ، تنعيم، scoring |
| `skeleton_norm.py` | نسخة مستقلة من دوال التطبيع (تنفع للـ import) |
| `notebook_fastdtw.py` | FastDTW على الفيديو **بالاعتماد على NTU** |
| `notebook_dollar1.py` | الـ $1 Recognizer ⬜ لسه ماتشغّلش |
| `pose_extract.py` | استخراج الـ keypoints محلياً (مخرجاته في `keypoints/`) |

### مسار الـ few-shot (من غير أي داتاسِت)

| الملف | الدور |
|---|---|
| **`run_crossvideo.py`** | ⭐ **الاختبار النظيف** — template من فيديو، اختبار على تاني |
| `oneshot_core.py` | نواة مشتركة: تمثيل، مسافات، عتبات، تصحيح hubness |
| `run_vidtest2.py` · `run_vidtest3.py` | تجارب فيديو واحد (4 و 13 حركة) |
| `run_oneshot.py` | تجربة sit/stand بكلاسين |
| `notebook_oneshot_fastdtw.py` | نسخة تشتغل جوّه نوتبوك Kaggle |

---

## 6. اللي لسه ناقص

```
☐ 1. شغّل الـ LSTM على vidtest2 و vidtest3 (على الـ ground truth الصح)
☐ 2. شغّل FastDTW (مسار NTU) على التلات فيديوهات
☐ 3. شغّل الـ $1 Recognizer على التلات فيديوهات
☐ 4. اعمل الجدول النهائي للمقارنة
☐ 5. صاحب المشروع يتفرّج على results/gt_vidtest1.mp4 ويأكّد الجدول
```

**تشغيل الـ baselines على Kaggle:**

```python
# بعد ما تشغّل خلايا الـ notebook الأصلي (عشان X_train_sk و all_keypoints يبقوا متحملين)
exec(open('notebook_fastdtw.py').read())
```

عشان تغيّر الفيديو، عدّل `VIDEO_NAME` في أول `notebook_fastdtw.py`. الـ fps
بيتظبط لوحده.

**تشغيل مسار الـ few-shot محلياً** (مش محتاج Kaggle ولا NTU):

```bash
python pose_extract.py && python run_crossvideo.py
```

---

## 7. تفاصيل تقنية سريعة

- **`vidtest3` بـ 30fps، و `vidtest1`/`vidtest2` بـ 60fps.** خلي بالك في أي
  حساب بالثواني.
- `FRAME_SKIP = 2` في الاستخراج — يعني رقم الـ keypoint = رقم فريم الفيديو ÷ 2.
- **إعدادات الـ inference:** `CONF_REJECT = 0.60` · moving average بـ `k=5` ·
  `MIN_SEGMENT = 2` · نوافذ 1.5/2/3/4 ثانية + mirror TTA · معايرة حرارية
  بـ grid search من 0.5 لـ 3.0 (الحالية 1.65).
- **التطبيع لازم يكون واحد في التدريب والفيديو** — أي اختلاف بيكسّر النتيجة.
- **الـ OpenCV بتاع pip مافيهوش H.264 شغّال.** لو هتكتب فيديو استخدم
  `imageio-ffmpeg` زي `render_gt.py`، وخلي الأبعاد **زوجية** (`libx264` +
  `yuv420p` بيفشل على أي بُعد فردي).
